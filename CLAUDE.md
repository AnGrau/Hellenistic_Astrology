# CLAUDE.md

Ce fichier donne à Claude Code le contexte du projet. Le lire entièrement avant toute action.

## Objectif du projet

Outiller la production d'analyses de thèmes natals en astrologie hellénistique (signes entiers), pour remplacer progressivement la saisie manuelle des données Astro-Seek/Astro.com et la génération manuelle du document Word par un pipeline reproductible :

1. calcul des positions et facteurs du thème (idéalement via une éphéméride locale, sans dépendre d'une capture d'écran),
2. dérivation automatique des données de la phase d'observation (maisons en signes entiers, secte, dignités essentielles, aspects ptoléméens par signe, Lots grecs, libération zodiacale),
3. génération du document `.docx` final selon la structure déjà validée sur les thèmes d'Anthony et de Liam.

Le dépôt sera versionné sur GitHub.

## Réglages astrologiques (non négociables, à appliquer systématiquement)

- Système de maisons : signes entiers (Whole Sign).
- Zodiaque tropical.
- Aspects ptoléméens uniquement (conjonction, sextile, carré, trigone, opposition), considérés par signe plutôt que par degré d'orbe — plus l'écart en degré est faible, plus l'aspect est actif.
- Aspects hors-signe ignorés, sauf si la planète la plus rapide applique à moins de 3° à la planète la plus lente. Cette proximité se mesure sur l'écart réel en longitude écliptique (les deux planètes doivent être proches de la frontière commune entre leurs signes adjacents), pas sur une coïncidence de degré-dans-signe : Mercure à 11°57' Sagittaire et Vénus à 11°51' Scorpion ont des degrés-dans-signe presque identiques mais restent à ~30° d'écart réel, car aucun des deux n'est proche de la frontière Scorpion/Sagittaire (cas vérifié sur le thème d'Anthony — la règle ne s'active pas).
- Solaire (retour solaire) : lieu de naissance, Soleil non précessé.
- Secte de la carte déterminée par la position du Soleil au-dessus ou au-dessous de l'horizon ; la Part de Fortune et la Part de l'Esprit changent de formule selon secte diurne/nocturne (piège fréquent à tester explicitement).
- Rôle de secte par planète : règle symétrique retenue pour le luminaire « en désaccord » avec la secte de la carte — toujours « Hors secte (jour) » ou « Hors secte (nuit) », jamais « Neutre » (un des deux thèmes de référence contenait une asymétrie sur ce point, corrigée après validation explicite).

## Méthode de rédaction en trois phases (à respecter dans tout générateur de texte ou de document)

1. **Observation** : relevé factuel des positions, secte, dignités essentielles, rétrogradations, planètes sous les rayons, aspects par signe — sans interprétation.
2. **Rédaction** : fiche technique descriptive (répartition élémentaire et modale, angularité, Ascendant et son maître, luminaires, dignités et réceptions, nœuds et Parts, structure du thème).
3. **Interprétation** : synthèse destinée au client (orientation générale, Ascendant et son maître, luminaires, structure du thème, nuances de secte/dignité, synthèse finale), avec section optionnelle sur les repères temporels actuels (libération zodiacale) si les dates le permettent.

## Style des textes générés

- Ton bienveillant mais factuel, jamais péremptoire sur la vie de la personne (formulations du type « invite à », « suggère », « mérite d'être vérifié avec la personne concernée »).
- Aucune émoticône.
- Toute affirmation technique (dignité, aspect, secte) doit être vérifiable depuis les données d'observation de la phase 1 — pas d'interprétation qui devance les faits.
- Bibliographie systématique en fin de document, avec URLs vérifiées à la date de génération (voir ci-dessous).

## Références bibliographiques de travail

- Chris Brennan, *Hellenistic Astrology: The Study of Fate and Fortune* — https://theastrologypodcast.com/books/
- Demetra George, *Ancient Astrology in Theory and Practice: A Manual of Traditional Techniques, Volume I: Assessing Planetary Condition* (Rubedo Press, 2019) — https://rubedo.press/ancient-astrology
- Demetra George, *Ancient Astrology in Theory and Practice: A Manual of Traditional Techniques, Volume II: Delineating Planetary Meaning* (Rubedo Press, 2022) — https://rubedo.press/ancient-astrology-volume-two
- Demetra George, *Astrology for Yourself* et *Astrology and the Authentic Self* — https://demetra-george.com/books/

Ces URLs pointent vers les pages officielles (éditeur ou auteur/autrice). Si le projet les republie (README, docs), revérifier leur validité avant chaque publication plutôt que de les recopier sans contrôle.

## Environnement de travail

- Développement exclusivement sur PC (pas de mobile, pas de dépendance à un environnement cloud propriétaire).
- Langage principal : Python (`uv` pour l'environnement/dépendances), layout `src/hellenistic_astrology/`.
- Éphéméride : **pyswisseph** (Swiss Ephemeris). Licence AGPL-3.0 acceptée pour l'instant ; si un service web/API public est un jour lancé, budgéter la licence commerciale Swiss Ephemeris Professional avant la mise en ligne. Fichiers de données `.se1` téléchargés via `scripts/fetch_ephemeris.py` vers `data/ephe/` (gitignored, jamais committés) ; en leur absence, le calcul bascule automatiquement sur la théorie Moshier/Moseph (précision ~1 arcseconde, suffisante pour les tests).
- **Licence du code de ce dépôt : AGPL-3.0-or-later** (fichier `LICENSE` à la racine, texte intégral et non modifié de la Free Software Foundation, avec seulement une ligne de copyright ajoutée en tête — pratique standard). Déclarée aussi dans `pyproject.toml` (`license = "AGPL-3.0-or-later"`, identifiant SPDX). Raison : ce dépôt importe directement `pyswisseph` (AGPL-3.0) et est public sur GitHub — d'après la doctrine GPL/FSF généralement admise, un projet qui importe une bibliothèque sous GPL/AGPL et distribue son propre code (un dépôt public cloneable en est une forme) doit distribuer ce code sous une licence compatible, généralement la même. Ce n'était pas fait avant ce jalon (dépôt public sans licence déclarée) — corrigé ici, indépendamment de toute décision Skill/Plugin/MCP (voir jalon correspondant pour le contexte qui a fait remonter cet oubli).
- Fuseau horaire : résolution automatique lieu + heure locale via `zoneinfo`, fiable à partir de 1916 (introduction de l'heure d'été en France) ; avant cette date, un datetime UTC explicite est obligatoire (voir `core/timezone.py`).
- Génération du `.docx` : décision actée d'utiliser **python-docx** (pas de sous-processus Node) — le script Node/docx-js évoqué initialement n'a jamais existé dans ce dépôt.
- CLI : `hellenistic-astrology <fichier-birth-data.json>` (entry point `src/hellenistic_astrology/cli.py`, déclaré dans `pyproject.toml`), voir `README.md`.
- Géocodage (`core/geocoding.py`) : opt-in uniquement — `latitude`/`longitude` explicites sont toujours prioritaires et n'entraînent aucun appel réseau ; le champ `place` (texte libre) n'est résolu via Nominatim/OpenStreetMap que s'ils sont absents. Invariant à préserver : ne jamais géocoder silencieusement quand des coordonnées directes existent déjà.
- Docker : laissé de côté explicitement pour l'instant (voir Reste à faire) ; aucun blocage technique, mais aucun besoin actif identifié non plus.

## Avant tout commit et push sur GitHub

Avant de committer et pousser, relire et mettre à jour si le jalon en cours l'exige :

- **CLAUDE.md** — au minimum la section État d'avancement (nouveau jalon) et, si le périmètre a changé, Reste à faire.
- **CLAUDE.md scopés** (`docgen/CLAUDE.md`, `interpretation/CLAUDE.md`) — si les conventions qu'ils documentent changent (nouveaux helpers, nouvelle sous-section, changement de mécanisme) ; l'historique jalon par jalon reste uniquement dans ce fichier racine.
- **README.md** — si l'usage du CLI ou une fonctionnalité visible par l'utilisateur a changé.
- **pyproject.toml** — si des dépendances ou la version ont changé.
- **Docs référencées depuis CLAUDE.md** (ex. `docs/table_termes_egyptiens.md`) — si leur contenu est concerné par le jalon.
- **Système de mémoire persistante** (`~/.claude/projects/.../memory/`, hors dépôt Git, jamais poussé sur GitHub) — mémoires feedback/project/user pertinentes au jalon.

C'est une consigne à appliquer soi-même, pas un garde-fou mécanique (pas de hook technique bloquant `git commit`/`git push` — décision explicite de l'utilisateur).

## État d'avancement

- **Jalon 1 (terminé)** : cœur de calcul (`src/hellenistic_astrology/core/`) — positions planétaires, maisons en signes entiers, secte, Part de Fortune/Esprit, résolution de fuseau horaire. Validé par `tests/test_regression_anthony.py` et `tests/test_regression_liam.py` (écarts sous 0,5' d'arc par rapport aux thèmes de référence).
- **Jalon 2 (terminé)** :
  - 2a — `core/dignities.py` (dignités essentielles domicile/exaltation/exil/chute/pérégrin, au niveau du signe ; table des maîtrises traditionnelles) et `core/sect.py` étendu avec le rôle de secte par planète.
  - 2b — `docgen/builder.py` génère la Phase 1 (Observation) en `.docx` via python-docx (tableaux positions + maîtrises), à partir de l'objet `Observation` uniquement. `scripts/generate_docx.py` permet une génération manuelle vers `output/` (gitignored). Phases 2 et 3 (rédaction, interprétation) restent hors périmètre de docgen : ce sont des tâches de génération de texte, pas de mise en forme de données.
  - Validé par `tests/test_dignities.py`, `tests/test_sect.py`, `tests/test_docgen.py` en plus des tests de régression.
- **Jalon 3 (terminé)** : `core/aspects.py` — regroupement des points en amas par signe, aspect ptoléméen par signe entre chaque paire d'amas, règle des 3° hors-signe basée sur la proximité écliptique réelle. Les 31 relations d'amas des deux thèmes (10 pour Anthony, 21 pour Liam) ont été vérifiées à la main contre le texte "Aspects par signe relevés" des documents de référence. Validé par `tests/test_aspects.py` et les tests de régression étendus.
  - **Limite connue** : aucun des deux thèmes de référence ne déclenche la règle des 3° (les deux documents confirment explicitement qu'elle ne s'active pas) — le cas positif n'est couvert que par un test synthétique, sans validation sur données réelles. À surveiller si un futur thème la déclenche.
- **Jalon 4 (terminé)** : `docgen/builder.py` génère la section "Aspects par signe relevés" en puces factuelles — une puce de conjonction par amas (accord grammatical masculin/féminin selon les membres), puis une puce par paire d'amas pour chaque relation d'aspect, aversions incluses. Format volontairement plus simple et déterministe que la prose des deux documents de référence, qui divergeaient stylistiquement entre eux sur ce point. Commentaire interprétatif (significations de maîtrise, etc.) toujours hors périmètre de docgen.
- **Jalon 5 (terminé)** : CLI réel (`src/hellenistic_astrology/cli.py`, entry point `hellenistic-astrology` déclaré dans `pyproject.toml`) — prend en entrée un fichier JSON de données de naissance (schéma libre, name/latitude/longitude + local_date/local_time/tz_name ou utc_datetime) et génère le `.docx` vers `output/<nom-slugifié>.docx` par défaut. `main.py` (scaffold `uv` mort, jamais branché) a été supprimé. Testé de bout en bout avec un client synthétique en plus des deux fixtures. Usage documenté dans `README.md`.
- **Jalon 6 (terminé)** : géocodage opt-in (`core/geocoding.py`) — un champ `place` (texte libre) en alternative à `latitude`/`longitude` dans le JSON du CLI, résolu via l'API Nominatim/OpenStreetMap (dépendance réseau, envoie le lieu à ce service tiers). `latitude`/`longitude` restent toujours prioritaires si fournis : aucun appel réseau dans ce cas, comportement par défaut inchangé. Testé avec des appels réseau mockés (`tests/test_geocoding.py`) et vérifié une fois en conditions réelles.
- **Jalon 7 (terminé)** : désambiguïsation du géocodage — `geocode()` demande 5 résultats à Nominatim et refuse (avec la liste des candidats) si plusieurs **lieux réellement distincts** sont trouvés, plutôt que de deviner en prenant le premier. "Distincts" est déterminé par une distance approximative (< 5 km = même lieu) plutôt qu'un simple comptage brut : test manuel en conditions réelles, "Paris, France" et "Paris 14e, France" renvoient chacun plusieurs entrées OSM quasi-identiques (ville + département, ou plusieurs commerces du même quartier) qui auraient déclenché une fausse ambiguïté sans ce filtre. "Paris" seul (sans pays) reste correctement détecté comme ambigu (France vs Texas). Nouveau champ optionnel `country_code` (ISO 3166-1 alpha-2) transmis à Nominatim pour lever l'ambiguïté en amont. Validé par `tests/test_geocoding.py` (dont un test dédié à la non-régression sur les faux positifs de proximité) et un test manuel en conditions réelles.
- **Jalon 8 (terminé)** : Nœud Nord/Sud (`ephemeris.north_node`, nœud **vrai** TRUE_NODE — le nœud moyen ne stationne jamais, incompatible avec les directions "Direct (rare)"/"Rétrograde (usuel)" observées dans les deux thèmes de référence) et Part d'Éros (`lots.part_of_eros`, formule Ascendant+Vénus−Esprit/Ascendant+Esprit−Vénus selon la secte, vérifiée exactement contre les deux thèmes). `Observation` gagne `north_node`/`south_node`/`part_of_eros` et un champ `all_points` (ordre canonique d'affichage, source unique pour docgen et pour le calcul des amas/aspects — refactor qui a remplacé la reconstruction manuelle de liste dans `docgen/builder.py`). Nœud Sud ajouté au cluster Lion d'Anthony bien que le document source ne le mentionne pas explicitement dans le texte d'aspects (incohérence avec Liam, qui mentionne "Nœud Sud" — traité comme une omission du document original plutôt que reproduit). Les 15 (Anthony) et 28 (Liam) relations d'amas ont été revérifiées à la main contre le texte des deux documents avant d'écrire les fixtures. Validé par les tests de régression étendus et `tests/test_docgen.py`.
- **Jalon 9 (terminé)** : réceptions mutuelles par domicile (`dignities.mutual_receptions_by_domicile`) — détecte les paires de planètes classiques chacune dans le domicile de l'autre. Rendu en `docgen` dans la section "Aspects par signe relevés" (même emplacement que le document source), phrasé simplement ("Réception mutuelle par domicile entre X et Y."). **Découverte en vérifiant contre les deux thèmes** : le calcul reproduit exactement l'unique réception confirmée d'Anthony (Mars/Vénus, aussi listée dans sa section Phase 2 "Dignités et réceptions"), mais détecte en plus une réception Vénus/Jupiter chez Liam (Vénus en Sagittaire, domicile de Jupiter ; Jupiter en Balance, domicile de Vénus) que la section "Dignités et réceptions" du document source de Liam ne mentionne pas du tout. Vérifiée indépendamment par calcul et retenue dans la fixture comme une omission probable du document original plutôt que reproduite à l'identique — cohérent avec la méthode déjà appliquée aux autres incohérences trouvées dans ces documents (secte Soleil/Lune, Nœud Sud d'Anthony). Validé par `tests/test_dignities.py` et les tests de régression étendus.
- **Jalon 10 (terminé)** : vérification bibliographique (`hellenistic_astrology/bibliography.py` + `scripts/check_bibliography.py`) — HEAD sur les 4 URLs de la section "Références bibliographiques de travail" de CLAUDE.md, avec repli en GET si le serveur ne supporte pas HEAD (405/501). Les 4 URLs sont codées en dur dans `bibliography.py` (pas de parsing de CLAUDE.md à l'exécution) mais `tests/test_bibliography.py::test_references_match_claude_md` lit CLAUDE.md à chaque run de test pour empêcher toute dérive entre les deux listes. Testé avec des appels réseau mockés et vérifié une fois en conditions réelles : les 4 URLs répondent, HEAD fonctionne directement (repli GET jamais déclenché). Script autonome, pas encore branché sur le CLI/docgen — la génération d'une vraie section "Bibliographie" dans le `.docx` n'existe pas encore (pas de fin de document tant que les Phases 2/3 ne sont pas traitées).
- **Jalon 11 (terminé)** : maîtres de triplicité (`dignities.triplicity_dignity`, `dignities.TRIPLICITY_RULERS`) — système **dorothéen** (Dorothée de Sidon, tel que documenté par Chris Brennan), distinct du système ptoléméen simplifié qui unifie le maître Eau jour/nuit sur Mars sans maître participant. **Aucune fixture de vérité terrain pour ce calcul** : ni le thème d'Anthony ni celui de Liam ne documentent la triplicité dans leurs documents de référence — vérifié à la place contre deux sources indépendantes et cohérentes entre elles (Wikipedia "Triplicity", Augurine) avant d'écrire la table, en particulier pour lever une incertitude réelle sur le maître participant de la triplicité d'Eau (Lune). Nouveau champ `PointPosition.triplicity_dignity`, calculé pour les 7 planètes classiques uniquement (comme `essential_dignity`), sans dépendance à la secte de la carte (les trois maîtres jour/nuit/participant sont tous considérés dignifiés ; la secte ne fait que hiérarchiser leur poids interprétatif, hors périmètre ici). **Pas encore rendu dans docgen** : la table des positions existante correspond exactement au tableau des documents de référence (qui n'inclut pas la triplicité), donc y ajouter une colonne est un choix de présentation à trancher séparément (voir Reste à faire). Validé par `tests/test_dignities.py` (5 tests, un par élément + un cas négatif) et une inspection manuelle informative des deux thèmes (non vérifiable, juste cohérente).
- **Jalon 12 (terminé)** : bornes égyptiennes (`dignities.EGYPTIAN_BOUNDS`, `dignities.egyptian_bound_ruler`, `dignities.bound_dignity`) — division de chaque signe en 5 segments inégaux (Vettius Valens, *Anthologiae* Livre I). **Recherche web infructueuse** pour obtenir une table complète et recoupable (blocage documenté avant ce jalon) ; **table finalement fournie et vérifiée par l'utilisateur** dans `docs/table_termes_egyptiens.md` (déplacé depuis `data/`, qui est réservé au cache d'éphéméride gitignored), avec ses propres sources et recoupements. Les 12 lignes totalisent chacune 30° (vérifié automatiquement par `tests/test_dignities.py::test_egyptian_bounds_each_sign_totals_30_degrees`). Même limite que la triplicité : aucune fixture Anthony/Liam ne documente les bornes, inspection manuelle des deux thèmes informative seulement. Nouveau champ `PointPosition.bound_dignity`, calculé pour les 7 planètes classiques.
- **Jalon 13 (terminé)** : décans/faces (`dignities.CHALDEAN_ORDER`, `dignities.decan_ruler`, `dignities.decan_dignity`) — division de chaque signe en 3 segments de 10°, maîtres selon l'ordre chaldéen (Saturne, Jupiter, Mars, Soleil, Vénus, Mercure, Lune), qui cycle en continu à travers les 36 décans du zodiaque sans se réinitialiser à chaque signe (Bélier 1er décan = Mars, tel qu'utilisé par Vettius Valens et Hephaistion). Contrairement aux bornes égyptiennes, la recherche web a abouti cette fois : vérifié contre deux sources indépendantes et cohérentes (une synthèse citant Valens/Hephaistion, une table complète des 36 décans d'Augurine), les 36 décans recoupés avant écriture du code. Même limite que triplicité/bornes : aucune fixture Anthony/Liam ne documente les décans. Nouveau champ `PointPosition.decan_dignity`, calculé pour les 7 planètes classiques. Validé par `tests/test_dignities.py` (36 décans paramétrés + limites + cyclage inter-signes) et une inspection manuelle informative des deux thèmes.
- **Jalon 14 (terminé)** : rendu docgen des dignités mineures — nouvelle table "Dignités mineures (triplicité, bornes, décans)" (`docgen.builder.add_minor_dignities_table`), insérée entre "Maîtrises traditionnelles" et "Aspects par signe relevés". Une ligne par planète classique, colonnes Triplicité/Terme/Décan, "—" pour les cellules sans dignité mineure (cohérent avec la convention déjà utilisée dans la table des positions). Table séparée plutôt qu'une extension de la table des positions existante, qui reste identique au tableau des documents de référence. Validé par un test unitaire dédié (valeurs synthétiques) et par le test de structure du document, qui recalcule les valeurs attendues via les mêmes fonctions `core.dignities` appliquées à la position réellement calculée (teste le branchement docgen, pas l'exactitude astrologique déjà couverte par `tests/test_dignities.py`).
- **Jalon 15 (terminé)** : libération zodiacale, calcul seul (`core/zodiacal_releasing.py`) — Part de Fortune uniquement, niveaux L1 à L4 complets, sans les raffinements de Valens (relâchement du lien, périodes culminantes) et sans rendu docgen (voir Reste à faire pour ces trois points, différés à la demande explicite de l'utilisateur). Table `ZODIACAL_RELEASING_YEARS` (années fixes par signe) et convention d'année « égyptienne » de 360 jours (12 mois de 30 jours, chaque niveau divisant l'unité du niveau parent par 12) vérifiées par recoupement de 3 sources indépendantes et cohérentes entre elles : transcription du podcast de Chris Brennan (épisode 192, citant directement Vettius Valens), calculateur Augurine, synthèse kerykeion — dont un exemple chiffré du podcast (période L1 Cancer de 25 ans débutant le 1/1/2012 se terminant le 22/8/2036, à cause des années de 360 jours) repris tel quel en test de non-régression. Point notable : Capricorne (27 ans) et Verseau (30 ans) ont des durées différentes en libération zodiacale bien que tous deux domicile de Saturne — particularité documentée de la technique, pas une erreur de recopie. Structure auto-similaire : `level_periods()` découpe un intervalle en périodes de signes consécutifs (cyclant plusieurs fois autour du zodiaque si nécessaire, dernière période tronquée pour boucler exactement) ; `sub_periods()` applique la même logique au niveau inférieur en partant toujours du signe du parent ; `releasing_tree()` calcule récursivement les niveaux 1 à `max_level`. Aucune fixture Anthony/Liam ne documente cette technique (comme pour triplicité/bornes/décans) ; validé par `tests/test_zodiacal_releasing.py` (table des années, exemple chiffré du podcast, invariants de partition exacte à tous les niveaux, cyclage multi-tour, recherche de période active).
- **Jalon 16 (terminé)** : libération zodiacale depuis la Part de l'Esprit — extension directe du jalon 15 : `core/zodiacal_releasing.py` était déjà générique sur le signe de départ (`releasing_tree(lot_sign, ...)` ne fait aucune hypothèse sur Fortune spécifiquement), donc aucun changement de code n'était nécessaire pour l'algorithme lui-même. Le travail de ce jalon est la couverture de test explicite de cet usage : `tests/test_zodiacal_releasing.py::test_l1_from_part_of_spirit_reference_charts`, paramétré sur les deux thèmes de référence (Esprit d'Anthony en Taureau, Esprit de Liam en Cancer), utilisant leur date de naissance réelle comme point de départ. Même limite que le jalon 15 : aucune fixture ne documente de périodes de libération zodiacale attendues, ces tests valident le branchement (signe de départ, date de naissance), pas des dates de résultat vérifiées indépendamment. Raffinements de Valens et rendu docgen restent différés (voir Reste à faire).
- **Jalon 17 (terminé)** : périodes culminantes ("peak periods"), `zodiacal_releasing.angular_signs_from()` et `is_peak_period()` — une période est culminante quand son signe est angulaire (1er/4e/7e/10e) par rapport à la Part de **Fortune**, y compris quand la séquence suivie est celle de l'Esprit (Fortune reste toujours la référence). Confirmé par une source primaire (transcription du podcast de Chris Brennan, épisode 192, citée dans le code) ; s'applique à tous les niveaux (L1 confirmé explicitement par Brennan, L2 corroboré par le calculateur Augurine). Fonctions pures et génériques (`ReleasingPeriod` inchangé, pas de champ ajouté) : `is_peak_period(period, fortune_sign)` s'applique à une période de n'importe quel niveau. Le relâchement du lien ("loosing of the bond"), recherché en même temps, a été différé (désaccord non résolu entre sources sur le signe de destination exact — voir Reste à faire pour le détail). Validé par `tests/test_zodiacal_releasing.py` (signes angulaires = groupe de modalité, périodes culminantes recoupées contre la Part de Fortune réelle des deux thèmes de référence, vérification explicite que la référence reste la Fortune même en suivant la séquence de l'Esprit).
- **Jalon 18 (terminé)** : rendu docgen de la libération zodiacale — nouvelle structure `zodiacal_releasing.ReleasingChapter` (un niveau 1 et ses sous-périodes de niveau 2 imbriquées, plus pratique à rendre que la liste plate de `releasing_tree()`) et `releasing_chapters()`. `core.chart.build_observation()` calcule désormais les chapitres Fortune et Esprit sur un horizon de 100 ans depuis la naissance (`ZODIACAL_RELEASING_HORIZON_YEARS`, limite pratique arbitraire de calcul, pas une contrainte de la technique) et les stocke dans deux nouveaux champs `Observation.zodiacal_releasing_fortune`/`zodiacal_releasing_spirit`. Deux nouvelles tables dans le document généré ("Libération zodiacale — Part de Fortune" et "— Part de l'Esprit"), niveaux L1+L2, colonnes Niveau/Signe/Maître/Début/Fin/Culminante — la colonne Culminante utilise toujours la Part de Fortune comme référence, y compris dans la table de l'Esprit (cohérent avec le jalon 17). Décisions de périmètre validées explicitement avec l'utilisateur avant l'implémentation : L1+L2 (pas L3/L4), les deux Parts, horizon de vie entière plutôt qu'un "chapitre actuel" daté du jour de génération (pas de dépendance à une date de référence "aujourd'hui"), période culminante affichée. Vérifié en conditions réelles (document généré pour Anthony : le premier chapitre commence bien en Scorpion comme sa Part de Fortune, la première sous-période L2 démarre sur le même signe que son L1 parent, le marquage culminant correspond au groupe de modalité attendu). Validé aussi par `tests/test_docgen.py` (table synthétique + structure complète recoupée contre `core.zodiacal_releasing` sur les deux thèmes de référence).
- **Jalon 19 (terminé)** : Phase 2 — Fiche technique, premières sous-sections ("Répartition élémentaire et modale", "Angularité"). Premier vrai changement de nature de docgen : au-delà des tables/puces à gabarit fixe, ces sous-sections assemblent plusieurs faits en phrases. Phase 2 étalée en plusieurs jalons (comme la libération zodiacale, jalons 15-18) — ce jalon ne couvre que les 2 sous-sections mécaniques ; les 4 sous-sections rédactionnelles restantes (Ascendant et son maître, Luminaires, Dignités et réceptions, Nœuds et Parts) nécessitent un générateur de phrases relationnelles pas encore conçu, voir Reste à faire.
  - **Nouvelles données** (`core/houses.py` : `MODALITY_BY_SIGN`, `house_quality()` ; `core/chart.py` : `PointPosition.element`/`.modality`/`.house_quality`, calculés pour tous les points — contrairement aux champs de dignité qui ne concernent que les 7 planètes classiques, l'Ascendant/les Nœuds/les Parts en ont besoin aussi). L'élément réutilise `dignities.SIGN_TRIPLICITY` (déjà la table sign→élément pour la triplicité), pas de nouvelle table.
  - **Constats issus de la lecture directe des deux documents de référence** (`References/*.docx`) plutôt que d'une supposition :
    1. Divergence de structure entre les deux documents sur cette sous-section, résolue en suivant Liam (validé avec l'utilisateur) : élément des 3 points directeurs (Ascendant/Soleil/Lune) *et* modalité de tous les points *avec comptage chiffré*, plutôt que la modalité seule d'Anthony.
    2. **Convention des 12 points** (Ascendant + 7 planètes + Nœud Nord + les 3 Parts, à l'exclusion du Milieu du Ciel et du Nœud Sud) confirmée dans les deux documents pour la répartition élémentaire/modale. Pour l'Angularité, Liam suit la même convention (son Milieu du Ciel tombe en maison 11, non angulaire — vérifié par calcul — et n'est mentionné ni parmi les points angulaires ni hors angularité) alors qu'Anthony **inclut** le Milieu du Ciel en maison 10 : à 3 attestations contre 1 (modal Anthony, modal Liam, angularité Liam), la convention d'exclusion uniforme est retenue, l'inclusion d'Anthony traitée comme une incohérence isolée (même méthode que pour les incohérences déjà rencontrées : secte, Nœud Sud, réception mutuelle).
    3. Incohérence mineure notée sans impact : le texte d'Anthony mentionne "trois des quatre Parts calculées" alors que le projet n'en calcule que 3 (Fortune/Esprit/Éros) — résidu de rédaction probable, sans conséquence puisque le nouveau gabarit ne reproduit pas ce texte mot pour mot.
  - **Simplifications assumées** (documentées plutôt que cachées) : la modalité est rendue en style « libellé technique » (3 catégories par ordre décroissant de comptage, sans jugement qualitatif du type « dominante très marquée »/« suivie de près par », qui ne généralise pas à un comptage arbitraire) — même choix que le jalon 4 pour les puces d'aspects. La phrase "Hors angularité" n'ajoute pas l'article "le" devant Soleil (contrairement à un unique passage du document de Liam, incohérent avec la convention déjà établie et testée en Phase 1 `DISPLAY_NAME_WITH_ARTICLE`) et ne fusionne pas les Parts groupées ("la Part de l'Esprit et la Part d'Éros", pas l'élision "et Éros" employée une fois par Liam). En revanche, la phrase "Maison N : ..." est reproduite **mot pour mot** contre le texte réel des deux documents (à la divergence du Milieu du Ciel près, constat 2 ci-dessus), listes à virgules simples sans "et" final, distinctes de la jonction `_join_french` déjà utilisée pour les puces d'aspects.
  - Validé par `tests/test_houses.py` (nouvelle table/fonction), des tests dédiés dans `test_regression_anthony.py`/`test_regression_liam.py` (élément/modalité/angularité de l'Ascendant contre le texte des documents), un test synthétique dans `test_docgen.py`, et `test_build_observation_document_structure` qui recoupe la phrase "Maison N" mot pour mot contre le texte réel de Liam et d'Anthony (moins le Milieu du Ciel). Vérifié aussi en conditions réelles (génération des deux documents).
- **Jalon 20 (terminé)** : Phase 2 — "Dignités et réceptions". En relisant précisément les 4 sous-sections rédactionnelles restantes, celle-ci s'est avérée être une liste à puces (comme les aspects, jalon 4) réutilisant presque entièrement des données déjà calculées (dignité essentielle, réception mutuelle, rétrogradation), sans besoin du générateur de phrases relationnelles — contrairement aux 3 autres (Ascendant et son maître, Luminaires, Nœuds et Parts), toujours différées.
  - **Nouvelle donnée** : combustion / « sous les rayons du Soleil » (seuil 15°, confirmé par les deux documents de référence sans cas litigieux) — déjà promise en Phase 1 par CLAUDE.md mais jamais implémentée avant ce jalon. `dignities.SolarProximity`/`dignities.solar_proximity()` (écart angulaire réel au Soleil, réutilise `aspects.angular_gap` via un import tardif pour éviter un cycle dignities → aspects → observation → dignities) ; `Observation.solar_proximity`, calculé dans `chart.py` pour les 5 planètes non-luminaires.
  - **Correction d'une erreur du jalon 19** : son entrée attribuait par erreur la détection d'éclipse à « Dignités et réceptions » — elle appartient en réalité à « Nœuds et Parts » (relu directement dans les documents). Corrigé dans Reste à faire.
  - **Constat de structure** (lecture directe des deux documents) : ordre des catégories de dignité confirmé identique (Chute, Exaltation, Exil (détriment), puis Pérégrins en dernier sans mention de signe — Domicile jamais illustré, ordre déduit par cohérence) ; jonction des listes à **virgules simples y compris pour 2 éléments** pour les catégories de dignité et les pérégrins (ex. Liam : "Mars (Taureau), Saturne (Lion)", pas "et"), mais jonction **avec "et"** (`_join_french`) pour réception/combustion/rétrogrades — deux conventions distinctes confirmées cohérentes chacune entre les deux documents, pas une divergence à trancher.
  - **Simplifications assumées** (même logique que les jalons 4/19) : pas de qualificatif subjectif "juste" pour la planète non combuste la plus proche du seuil (Anthony : "Vénus est juste hors..." → "Vénus est hors...") ; titre "Rétrogrades" invariant, sans accord singulier/pluriel ni qualificatif "(parmi les sept planètes traditionnelles)" (Liam).
  - Validé par `tests/test_dignities.py` (`solar_proximity` sur longitudes synthétiques), des tests dédiés dans `test_regression_anthony.py`/`test_regression_liam.py` (écarts au Soleil recoupés contre les valeurs chiffrées exactes des deux documents : Mercure 13°46'/Jupiter 9°08'/Vénus 16°20' pour Anthony, Jupiter 2°18'/Mercure 19°41' pour Liam, tolérance 0,1°), un test synthétique dans `test_docgen.py`, et `test_build_observation_document_structure`. Vérifié aussi en conditions réelles : les puces générées reproduisent quasi mot pour mot le texte des deux documents (aux simplifications assumées près).
- **Jalon 21 (terminé)** : Phase 2 — "Ascendant et son maître", première sous-section rédactionnelle nécessitant une vraie relation décrite en phrase (pas une liste mécanique). Aucun nouveau calcul `core/` : tout réutilise `Observation.ascendant`/`rulerships`/`clusters`/`planet()` déjà existants.
  - **Deux helpers réutilisables** dans `docgen/builder.py`, pensés pour les 2 sous-sections rédactionnelles restantes (Luminaires, Nœuds et Parts) : `_conjunction_clause(point, observation)` (« conjoint(e) à X, Y » pour les co-résidents de signe, None si seul) et `_rulership_aspect_clause(ruler, governed_sign)` (relation d'aspect entre le signe du maître et un signe qu'il gouverne, None si déjà dans ce signe).
  - **Constats de la relecture directe des deux documents** : (1) ordre signe/maison inversé entre les deux ("situé en maison 4 (Scorpion)" chez Anthony vs "situé en Lion, maison 7" chez Liam) — retenu signe-puis-maison, cohérent avec le reste du projet ; (2) la relation d'aspect maître↔signe de l'Ascendant est mentionnée par Liam (opposition) mais **absente du texte d'Anthony** alors que Scorpion/Lion est un carré non trivial — traité comme une omission isolée d'Anthony (même méthode que les incohérences précédentes), la relation est désormais **toujours** incluse sauf quand le maître est déjà dans le signe de l'Ascendant.
  - **Simplifications assumées** : pas d'article/genre devant le signe gouverné ("avec Verseau" pas "avec le Verseau" — éviterait une table de genre des signes, français a des signes masculins/féminins/pluriels) ; le pronom "qu'il/qu'elle gouverne" reste accordé, mais au genre de la planète (déjà dans `FEMININE_PLANETS`), pas du signe ; suppression du qualificatif "exacte" (même logique que jalons 4/19/20) ; dignité raccourcie ("Exil (détriment)" → "en exil", "Pérégrin(e)" → "pérégrin(e)" sans "en"), nouvelle table `DIGNITY_INLINE_CLAUSE` distincte de `DIGNITY_LABELS` (jalon 20, qui garde la forme longue pour les puces).
  - Validé par 3 tests synthétiques dans `test_docgen.py` (domicile unique + aspect + conjonction ; double domicile + aversion + seul dans son signe, cas non illustré par les documents ; maître dans le signe de l'Ascendant lui-même, pas de clause d'aspect) et `test_build_observation_document_structure`, qui recoupe le texte généré **mot pour mot** contre les deux documents de référence (aux divergences assumées ci-dessus près). Vérifié aussi en conditions réelles.
- **Jalon 22 (terminé)** : Phase 2 — "Luminaires" (Soleil, Lune) et phase de lunaison natale. Trois choix de périmètre validés explicitement avec l'utilisateur avant l'implémentation (voir méthode habituelle) :
  1. La Lune énumère toujours ses relations d'aspect à tous les autres amas (`docgen.builder._cluster_relations_clause`, réutilisant `observation.cluster_aspects`) ; le Soleil ne le fait jamais, remplacé par une clause de combustion de son propre point de vue (`_solar_rays_clause`, réutilise `dignities.solar_proximity` déjà calculé au jalon 20) — confirmé net dans les deux documents de référence (le Soleil n'énumère aucune relation d'aspect dans aucun des deux, la Lune les énumère dans les deux).
  2. La mention "lumière de secte de ce thème diurne/nocturne" (`_sect_light_clause`, réutilise `PointPosition.sect_role` et `Observation.sect`) est désormais systématique pour le luminaire en accord avec la secte de la carte, alors qu'un seul des deux documents l'explicite (Anthony, pour sa Lune nocturne — absente du Soleil diurne de Liam, qui devrait pourtant la porter aussi).
  3. La phase de lunaison natale (8 phases de Dane Rudhyar, *The Lunation Cycle*, 1967) est implémentée maintenant plutôt que différée à nouveau — `core/lunation.py`, division en 8 segments égaux de 45° de l'écart Lune-Soleil, vérifiée contre deux sources indépendantes et cohérentes entre elles (Cafe Astrology, synthèses françaises reprenant la même terminologie "disséminatrice"/"balsamique"/etc.) faute de fixture de vérité terrain fiable pour cette technique.
  - **Deux incohérences découvertes par vérification indépendante, non reproduites** (même méthode que les précédentes — secte, Nœud Sud, MC, réception mutuelle) :
    - Le thème d'Anthony affirme dans Luminaires "sous les rayons de personne (il est la source)" pour le Soleil, alors que sa propre section "Dignités et réceptions" (vérifiée au jalon 20, valeurs exactes) liste Mercure et Jupiter sous les rayons du Soleil — contradiction interne au même document. Le calcul (`solar_proximity`) est réutilisé tel quel, le rendu suit donc la donnée vérifiée plutôt que la phrase Luminaires d'Anthony.
    - Le thème de Liam qualifie son écart Soleil-Lune (~206,5°, ~26° après l'opposition exacte) de "disséminatrice" ; les bornes des 8 phases, vérifiées indépendamment et qui reproduisent exactement la phrase d'Anthony (écart ~269,5°, à 0,5° de la frontière, décrit comme "à la limite du dernier quartier"), classent ~206,5° en "Pleine". Traité comme une erreur isolée du document de Liam, pas comme un système de bornes différent à arbitrer (voir `core/lunation.py` pour le détail du raisonnement).
  - Convention de nommage des amas cibles dans la clause relationnelle de la Lune : "l'amas du Signe" pour un amas à plusieurs membres, nom du membre seul sinon — reprend la convention déjà établie par `cluster_aspect_text` (Phase 1, jalon 4) plutôt que le style "Planète/Planète" employé une fois par Liam. Regroupement par type d'aspect dans l'ordre canonique de `ASPECT_LABEL` (pas l'ordre libre de la prose des documents), simplification cohérente avec celles déjà actées aux jalons 4/19/20/21.
  - Validé par `tests/test_lunation.py` (bornes des 8 phases, enroulement de l'écart, recoupement des écarts exacts d'Anthony et de Liam), deux tests synthétiques dans `test_docgen.py` (Soleil sans combustion + Lune avec relations groupées ; Soleil avec combustion + Lune sans mention de secte) et `test_build_observation_document_structure`, qui recoupe le texte généré **mot pour mot** contre les deux documents de référence, aux trois divergences assumées ci-dessus près. Vérifié aussi en conditions réelles (génération des deux documents).
- **Jalon 23 (terminé)** : libération zodiacale — relâchement du lien ("loosing of the bond"), point bloqué depuis le jalon 17 par un désaccord non résolu entre sources sur le signe de destination exact. Résolu par l'utilisateur, qui a trouvé la source décisive (Chris Brennan, « Annual Profections, Lots, and Zodiacal Releasing », *The Mountain Astrologer*, avril/mai 2012) et fourni un exemple chiffré réel pour les deux thèmes de référence.
  - **Règle implémentée** dans `zodiacal_releasing.level_periods()` (fonction déjà partagée par tous les niveaux via `sub_periods`/`releasing_tree`, donc récursive sans code spécifique par niveau) : si le cycle des 12 signes reviendrait sur `start_sign` avant la fin de la durée à découper, la période suivante saute au signe **opposé à `start_sign`** (et non opposé au dernier signe visité, comme l'indique à tort aswinsubramanyan.com/zodiacalreleasing.net — traité comme une erreur de cette source) puis l'ordre zodiacal normal reprend. Le seuil ">17,5 ans" déjà noté au jalon 17 n'est pas codé en dur : il émerge naturellement de la boucle (un tour complet des 12 signes totalise toujours 211 "années" de la table quel que soit le signe de départ, donc ~17,3-17,4 ans à l'unité du niveau courant — seuls les signes dont la durée propre dépasse ce total, tous maîtrisés par Mercure/Lune/Soleil/Saturne, peuvent effectivement bloquer avant la fin de leur propre période).
  - **Vérification indépendante** : confirmé par plusieurs sources cohérentes entre elles (Mountain Astrologer, qui donne l'exemple chiffré exact Capricorne → ... → Sagittaire → [saut] Cancer ; The Astrology Podcast/lignée Brennan) — et surtout par **calcul direct sur les deux thèmes de référence** (non documenté dans les `.docx`, qui ne détaillent pas l'historique de libération zodiacale, mais vérifiable en calculant les périodes L1/L2 de la Part de Fortune) : le cycle L2 d'Anthony (Capricorne, L1 1997-07-01 → 2024-02-10) boucle exactement le 30/10/2014 et saute vers le Cancer (opposé du Capricorne) ; celui de Liam (Lion, L1 2005-10-19 → 2024-07-11) boucle exactement le 17/02/2023 et saute vers le Verseau (opposé du Lion) — dates et signes reproduits au jour près par le calcul, avant toute correction de code (seul le signe de la période suivante changeait, pas les dates de bouclage elles-mêmes, déjà correctes depuis le jalon 15).
  - Nouveau champ `ReleasingPeriod.bond_loosed: bool = False`, vrai uniquement pour la période immédiatement issue du saut (pas les occurrences ultérieures normales du signe opposé) — nécessaire ici (contrairement aux périodes culminantes, jalon 17, qui restent une fonction pure) car l'information dépend de la séquence de génération, pas seulement du signe et d'un paramètre externe.
  - Validé par `tests/test_zodiacal_releasing.py` : saut vers le signe opposé (cas générique Verseau/Lion), absence de saut quand le signe de départ ne peut pas boucler (Bélier), récursivité à un niveau plus profond (L3), et les deux dates/signes exacts d'Anthony et de Liam en cas de non-régression. Vérifié aussi en conditions réelles (les deux documents régénérés reflètent la correction dans la table "Libération zodiacale — Part de Fortune").
- **Jalon 24 (terminé)** : rendu docgen du relâchement du lien — nouvelle colonne "Relâchement du lien" dans la table "Libération zodiacale" (`docgen.builder.ZODIACAL_RELEASING_HEADER`/`_zodiacal_releasing_row`), affichant "Oui" pour la période directement issue d'un saut (`ReleasingPeriod.bond_loosed`, jalon 23) et "—" sinon — même convention que la colonne "Culminante" déjà en place. Aucune nouvelle logique de calcul : le champ était déjà exposé, seul le rendu manquait. Validé par un test synthétique dans `test_docgen.py` (un cas "Oui", un cas "—") et vérifié en conditions réelles : le document régénéré d'Anthony affiche bien "Oui" sur la période Cancer du 30/10/2014, ainsi qu'un second relâchement du lien plus tardif (Lion, 2041, également marqué culminant) qui n'avait pas été observé lors de la vérification du jalon 23.
- **Jalon 25 (terminé)** : Phase 2 — "Nœuds et Parts", dernière sous-section rédactionnelle de la Fiche technique (calc + docgen dans le même jalon, comme les jalons 19-22).
  - **Nouveau calcul** : `core/eclipse.py` (`eclipse_configuration`, `EclipseConfiguration`) — un thème est en "configuration d'éclipse" si la Lune est simultanément proche d'un Nœud ET proche de la syzygie Soleil-Lune correspondante (conjonction pour une éclipse solaire, opposition pour une éclipse lunaire), aux limites astronomiques standard d'éclipse partielle (18,4° solaire / 12,2° lunaire). Seuil validé explicitement avec l'utilisateur (`AskUserQuestion`, 3 options sourcées) : ni Anthony ni Liam n'ont de configuration d'éclipse (les deux échouent largement sur au moins un des deux critères), donc ce choix précis n'est pas calibrable sur un cas positif réel — seule la cohérence avec les limites astronomiques standard le justifie, comme pour la triplicité/les bornes/les décans en leur temps. Nouveau champ `Observation.eclipse`, calculé dans `chart.py` (import tardif `eclipse -> aspects`, même contournement de cycle que `dignities.solar_proximity`).
  - **Trois paragraphes docgen**, réutilisant les helpers déjà existants plutôt que d'en ajouter :
    1. "Axe des Nœuds" — signe/maison des deux Nœuds, conjonction de signe éventuelle pour chacun (`_conjunction_clause`, jalon 21), et proximité réelle au Nœud le plus proche de la Lune quand elle dépasse le seuil solaire (notable même sans éclipse complète — cas d'Anthony, Lune à 0°33' du Nœud Sud mais très loin de toute syzygie).
    2. Relation Fortune/Esprit — relation d'aspect par signe entre les deux Parts (`aspects.sign_aspect`, Phase 1), phrasée en aversion explicite si adjacentes ou en aspect nommé sinon.
    3. Configuration d'éclipse — gabarit unique donnant les deux écarts chiffrés (Lune-Nœud, Soleil-Lune par rapport à la syzygie la plus proche), que la configuration soit positive ou non ; branche positive non illustrée par les deux thèmes de référence, testée uniquement en synthétique.
  - **Simplifications assumées** (même logique que les jalons précédents) : les conjonctions de signe sont désormais toujours calculées pour les deux Nœuds (le document d'Anthony n'en mentionne qu'une, Nœud Sud/Ascendant-Lune, omettant Nœud Nord/Éros qui existe pourtant) ; le Nœud le plus proche de la Lune est nommé explicitement plutôt que la formulation ambiguë "quasiment à l'opposition" du document d'Anthony (qui décrit la même relation géométrique de deux façons différentes selon qu'on la lit depuis le Nœud Nord ou le Nœud Sud) ; le paragraphe d'éclipse suit un gabarit déterministe unique plutôt que la prose libre et divergente des deux documents sources.
  - Validé par `tests/test_eclipse.py` (cas synthétiques éclipse solaire/lunaire exacts, non-éclipse par échec de chaque critère indépendamment, et les écarts exacts recoupés contre les deux thèmes de référence), deux tests synthétiques dans `test_docgen.py` (un cas riche avec conjonctions+proximité+éclipse positive, un cas sans conjonction/proximité/éclipse avec aversion) et `test_build_observation_document_structure`, qui recoupe le texte généré **mot pour mot** contre les deux documents de référence, aux simplifications assumées ci-dessus près. Vérifié aussi en conditions réelles (génération des deux documents).
  - Ceci clôt la Phase 2 — Fiche technique dans son périmètre initial (les 6 sous-sections des jalons 19-25) ; voir Reste à faire pour "Structure du thème", qui restait explicitement hors de ce périmètre.
- **Jalon 26 (terminé)** : Phase 3 — assemblage de brief pour rédaction assistée. Avant d'attaquer "Structure du thème" (jalon initialement prévu comme reliquat de Phase 2), la relecture directe des deux documents de référence a montré que c'est en réalité la 4e sous-section de Phase 3 — Interprétation (8 sous-sections réelles : intro sans titre identique mot pour mot dans les deux documents, Orientation générale, L'Ascendant et son maître, Les luminaires, La structure du thème, Nuances de secte/dignité (et de mouvement chez Anthony), Synthèse, Repères temporels actuels, Limites de cette analyse) — correction de roadmap actée séparément avant ce jalon (voir commit correspondant), Phase 2 déclarée entièrement terminée.
  - **Mécanisme validé explicitement avec l'utilisateur** (question posée : brief déterministe + rédaction assistée / appel API automatisé dans le CLI / Skill Claude dès maintenant) : **brief factuel déterministe et testé, rédaction assistée hors CLI** — pas d'appel API automatisé (coûteux, non reproductible, en tension avec l'objectif de pipeline reproductible du projet), cohérent avec le texte des documents eux-mêmes ("gagnera à être discuté avec la personne concernée"). Sortie en texte brut/Markdown portable (voir ci-dessous pourquoi ce choix n'est plus lié qu'à la stabilité du format, pas à la portabilité).
  - **Empaquetage en Skill (ou plugin Claude Code, alternative non tranchée) différé**, mais avec une correction factuelle importante par rapport à ce que ce document affirmait : un Skill Claude (`SKILL.md`) n'est **pas** spécifique à Anthropic — vérifié via `gh api repos/mistralai/mistral-vibe/issues/189/comments` (pas une simple lecture web, contenu exact récupéré) : Mistral Vibe lit le même format `SKILL.md` depuis sa version 1.3.0 (23/12/2025), cherché dans `skill_paths` custom, `./.vibe/skills/`, ou `~/.vibe/skills/` — seul le chemin d'installation diffère entre outils, pas le format lui-même ; `agentskills.io`, spec ouverte publiée par Anthropic, vise à unifier ce point entre outils (un commentaire ultérieur du même fil note que même le nom du dossier diverge encore selon les outils — OpenCode utilise `.opencode/skill/`). Ceci ne change pas l'ordre décidé : empaqueter reste un jalon ultérieur, une fois le format de brief éprouvé sur des rédactions réelles — la portabilité de `SKILL.md` entre outils n'apporte aucun gain à le faire maintenant plutôt que plus tard, seulement un risque de devoir réécrire l'emballage si le format de brief évolue.
  - **Nouveau package `interpretation/`** (parallèle à `core/` et `docgen/`, nature de tâche différente) : `interpretation/brief.py`, fonction `build_interpretation_brief(observation, birth, when=None)`. Réutilise au maximum les sections Phase 2 déjà testées comme socle factuel (rejoue `add_elemental_modal_section`/`add_angularity_section`/`add_ascendant_and_ruler_section`/`add_luminaries_section`/`add_aspects_section`/`add_dignities_and_receptions_section` de `docgen.builder` contre un `Document()` jetable et récupère le texte des paragraphes produits) : garantit zéro divergence avec la Phase 2 et très peu de code nouveau. Faits réellement nouveaux, petits : période de libération zodiacale active *aujourd'hui* pour Fortune et Esprit (réutilise `zodiacal_releasing.active_period`, déjà testé au jalon 17, sur les chapitres L1+L2 déjà calculés au jalon 18 — vérifié en conditions réelles : les dates générées pour le "aujourd'hui" du 04/07/2026 correspondent presque exactement à celles du "Repères temporels actuels (juillet 2026)" des deux documents de référence, à un jour près par endroits) ; clause de précision de l'heure de naissance (avec repli si seul `utc_datetime` est fourni) ; rôle de secte par planète listé isolément (donnée déjà calculée, jamais listée seule ailleurs). Le brief s'ouvre par un rappel compact des règles de style de CLAUDE.md et se termine par les 4 références bibliographiques déjà vérifiées (`bibliography.py`, jalon 10), pour rester autoportant si collé tel quel dans une interface tierce sans accès à CLAUDE.md.
  - **`scripts/generate_brief.py`**, miroir exact de `scripts/generate_docx.py` (mêmes fixtures), écrit `output/<nom>_brief.md` (gitignored).
  - Validé par `tests/test_interpretation_brief.py` (présence des 8 sous-sections + bibliographie, recoupement mot pour mot de faits Phase 1/2 déjà validés pour confirmer la réutilisation sans divergence, les deux branches de la clause d'heure de naissance, période de libération zodiacale active synthétique dans et hors horizon) et vérifié en conditions réelles (génération des deux briefs, dates de libération zodiacale actuelles recoupées contre le texte des documents de référence).
  - **Suite immédiate (même session)** : rédaction assistée réelle des deux briefs, gardée telle quelle par décision explicite de l'utilisateur (`output/anthony_phase3_draft.md`, `output/liam_phase3_draft.md`) — chaque affirmation vérifiée contre le brief correspondant avant écriture, pas de fait recalculé de mémoire. L'utilisateur a précisé réutiliser tout le contenu de `/output` manuellement dans des sessions Claude Chat et Mistral Le Chat/Vibe : consigne ajoutée dans les garde-fous de ne jamais supprimer de fichier dans `/output`, seulement le régénérer.
- **Jalon 27 (terminé)** : décision Skill vs plugin pour la rédaction assistée de Phase 3 (item 1 de Reste à faire). Recherche faite avant de trancher (documentation officielle Claude Code, `code.claude.com/docs/en/skills`, récupérée le jour même — pas une mémoire non revérifiée) : un Skill est un dossier `SKILL.md` (frontmatter `name`/`description`, fichiers annexes optionnels) suivant le standard ouvert Agent Skills (`agentskills.io`), fonctionnant à l'identique sur Claude.ai/Claude Code/l'API et, depuis sa v1.3.0, sur Mistral Vibe (jalon 26) ; les skills de projet vivent dans `.claude/skills/<nom>/SKILL.md` et sont committés au dépôt (page officielle : "Project skills: commit `.claude/skills/` to version control"). Un plugin Claude Code, à l'inverse, est une couche d'empaquetage *spécifique à Claude Code* (marketplace, bundle de skills + hooks + subagents + MCP servers + slash commands) qui n'apporte **aucune portabilité supplémentaire** vers Claude Chat ou Mistral Vibe/Le Chat par rapport à un skill seul.
  - **Décision** : Skill de projet, pas plugin — vu l'objectif explicite (Claude Chat + Claude Code + Mistral Vibe/Le Chat), le plugin n'aurait rien apporté ici ; il reste une option pour plus tard si un besoin Claude-Code-spécifique apparaît (slash command, MCP server distant, hooks), mais rien de tel n'est nécessaire aujourd'hui.
  - **`.claude/skills/hellenistic-astrology-phase3/SKILL.md`** : reprend et généralise la rédaction assistée faite à la main pour Anthony et Liam (jalon 26) — règles de style, structure des 9 blocs (intro sans titre + 8 sous-sections nommées), règle de traçabilité stricte au brief fourni (aucun fait recalculé de mémoire), et un chemin de repli optionnel (proposer de lancer `scripts/generate_brief.py` si l'environnement a un accès Bash/dépôt mais qu'aucun brief n'est encore fourni). **Décision explicite avec l'utilisateur** (`AskUserQuestion`) : pas d'exemple de rédaction few-shot dans le skill — les brouillons réels d'Anthony/Liam contiennent l'analyse de personnes réelles et ne peuvent pas être committés dans un skill de projet (même règle que `/References`, `/tests/fixtures`, `/output`, déjà dans `.gitignore`) ; un exemple synthétique aurait demandé d'inventer un thème cohérent pour un gain incertain, alors que le brief lui-même embarque déjà les règles de style (jalon 26).
  - **Distribution** : Claude Code découvre le skill automatiquement depuis `.claude/skills/` (aucune action requise). Mistral Vibe le lira une fois copié ou symlinké vers `.vibe/skills/` (ou référencé via `skill_paths` dans sa config) — à la charge de l'utilisateur, pas un script du dépôt. Claude.ai (Claude Chat, hors Claude Code) charge les skills via son propre mécanisme d'activation (Settings/Capacités), pas par découverte de dossier : upload manuel du dossier skill par l'utilisateur, non automatisable depuis ce dépôt.
  - Aucun test Python : pas de code `src/` touché, `SKILL.md` est un fichier d'instructions. Vérification prévue par usage réel (invoquer le skill en session Claude Code avec un brief déjà généré).
- **Jalon 28 (terminé)** : serveur MCP local pour exposer le calcul (Phase 1/2) et le brief de Phase 3 (jalon 26) à Claude Code et Mistral Vibe, distinct du jalon 27 (skill de *rédaction*, qui part d'un brief déjà calculé).
  - **Recherche de licence faite avant tout code** (pas une supposition) : MCP a deux modes de transport aux conséquences radicalement différentes — local (stdio, sous-processus, aucun réseau) vs distant (Streamable HTTP, hébergé publiquement). Confirmé pour Claude.ai (support.claude.com, y compris les plans individuels Pro/Max) et Mistral Le Chat (docs.mistral.ai) : leurs connecteurs personnalisés exigent un serveur MCP **joignable publiquement sur Internet** — un serveur derrière un pare-feu ou en réseau privé ne fonctionne pas. Un tel serveur distant exposant `pyswisseph` publiquement serait exactement le « service web/API public » que CLAUDE.md (section Environnement de travail) réserve déjà derrière la clause de licence Swiss Ephemeris Professional à budgeter avant mise en ligne.
  - **Question posée explicitement à l'utilisateur** (`AskUserQuestion`, rejetée par l'utilisateur qui a répondu directement en texte — voir note ci-dessous) puis affinée en échange direct : local uniquement, et seulement si aucun problème de licence ne se pose. Vérification faite sur le texte de la GNU AGPL-3.0 elle-même (section 13, "Remote Network Interaction") : l'obligation de fourniture du code source ne se déclenche qu'en cas d'interaction réseau avec des **utilisateurs distants** (le cas SaaS que l'AGPL ferme). Un serveur MCP stdio local, lancé par Claude Code ou Mistral Vibe sur la machine de l'utilisateur lui-même, est structurellement identique à l'exécution du CLI en local — déjà couvert par la décision « Licence AGPL-3.0 acceptée pour l'instant ». Aucun nouveau problème de licence pour ce jalon ; l'hébergement public reste explicitement hors périmètre, non engagé.
  - **`src/hellenistic_astrology/mcp_server.py`** : instance `FastMCP` (paquet `mcp`, nouvelle dépendance), trois outils, chacun une fine couche `@mcp.tool()` au-dessus d'une fonction Python nue et testable sans dépendre du SDK MCP : `compute_observation` (JSON structuré via `Observation.to_json`, déjà existante mais jamais exposée hors des tests), `generate_document` (`.docx` complet, réutilise `docgen.builder.build_observation_document` et `cli.slugify`, déjà public), `generate_interpretation_brief` (réutilise `interpretation.brief.build_interpretation_brief`, jalon 26 — écrit aussi dans `output/` par cohérence avec `scripts/generate_brief.py`). Même gestion d'erreur que le CLI (`GeocodingError`/`ValueError` capturées, message `erreur : ...`).
  - **`.mcp.json`** committé à la racine (aucune donnée personnelle, juste le câblage du serveur) : Claude Code le découvre automatiquement (approbation demandée au premier lancement, comportement de sécurité normal). Mistral Vibe nécessite d'ajouter la même commande dans sa propre configuration (non committée ici, documentée dans `README.md`).
  - Validé par `tests/test_mcp_server.py` (les trois fonctions nues testées directement, pas le protocole MCP — JSON recoupé contre les fixtures Anthony/Liam déjà validées, `.docx` généré relisible par python-docx, brief contenant les en-têtes attendus, gestion d'erreur sur données de naissance incomplètes) et vérifié en conditions réelles à deux niveaux : `mcp.list_tools()` confirme les 3 outils enregistrés, puis connexion réussie depuis **Claude Desktop** (Windows + WSL2, distinct de Claude Code) après débogage en session — Claude Desktop ne découvre pas `.mcp.json` automatiquement, il faut l'ajouter à sa config globale (`%APPDATA%\Claude\claude_desktop_config.json`, via un pont `wsl.exe -d <distro> -- bash -lc "cd <dépôt> && uv run ..."`) ; premier essai échoué parce que la clé `mcpServers` avait été insérée dans un objet de préférences imbriqué au lieu de la racine du JSON — corrigé, reconnecté avec succès. Détail complet dans `README.md`.
- **Jalon 29 (terminé)** : `CLAUDE.md` scopés pour `docgen/` et `interpretation/` (item 1 de Reste à faire), maintenant que le skill (jalon 27) et le serveur MCP local (jalon 28) existent et que leur portée réelle est mieux cernée.
  - **`src/hellenistic_astrology/docgen/CLAUDE.md`** : frontière stricte (aucun calcul, aucune interprétation), les deux styles de rendu (tables/puces factuelles vs phrases relationnelles avec les trois helpers réutilisables `_conjunction_clause`/`_rulership_aspect_clause`/`_cluster_relations_clause`), la norme des « simplifications assumées » (un gabarit déterministe plutôt que la variabilité de la prose des deux documents de référence), convention de tests. L'historique jalon par jalon (2b, 4, 18-25) reste uniquement dans ce fichier racine, pour éviter la dérive entre deux sources.
  - **`src/hellenistic_astrology/interpretation/CLAUDE.md`** : pourquoi ce module existe séparément de `docgen` (Phase 3 = prose interprétative, pas gabarit déterministe — décision de mécanisme du jalon 26), tableau de correspondance sous-section → fonction `docgen` réutilisée, ce qui reste volontairement fixe dans le brief (rappel de style, bibliographie), convention de tests.
  - **`.claude/skills/hellenistic-astrology-phase3/CLAUDE.md` délibérément absent** : le `SKILL.md` de ce dossier joue déjà ce rôle en entier (règles de style, structure des 8 sous-sections, règle de traçabilité) — un second fichier de documentation dans le même petit dossier aurait fait doublon plutôt qu'aidé.
  - Pas de nouveau code ni de nouveau test (fichiers `CLAUDE.md`, pas de logique) ; `uv run pytest` reste vert par construction.
- **Jalon 30 (terminé)** : assembler le document final (item 4 de Reste à faire) — colle la prose de Phase 3 (rédigée en session assistée, jalons 26-27) à la suite du `.docx` de Phase 1/2 déjà généré.
  - **Mécanisme validé explicitement avec l'utilisateur** : l'utilisateur finalise/édite le texte Markdown de Phase 3 (dans le chat ou dans son fichier `.md`) jusqu'à ce qu'il soit prêt, *puis* lance l'assemblage une seule fois — le `.docx` final résultant n'est plus retouché à la main. Pas de logique de « ne pas écraser des éditions manuelles » à prévoir (contrairement à l'alternative « insérer puis éditer dans Word », écartée).
  - **Vérification technique faite avant le code** : ouvrir un `.docx` déjà généré, y ajouter des titres/paragraphes à runs mixtes/puces, sauvegarder et relire — testé directement sur `output/anthony.docx` avant d'écrire quoi que ce soit. Le sous-ensemble Markdown réellement produit par le skill de rédaction (jalon 27), observé dans les deux brouillons déjà écrits, est étroit et borné : `#`/`##` (titres), paragraphes de texte brut, puces `- `, emphase `*texte*` (italique) — aucun gras, aucun code inline. Pas de dépendance Markdown générale : un parseur ad hoc pour ce sous-ensemble précis suffit et reste simple à tester exhaustivement.
  - **Nouveau module `src/hellenistic_astrology/assembly.py`** (nature de tâche distincte de `docgen`, qui part toujours d'un `Observation` frais et jamais d'un `.docx` déjà existant sur disque) : `parse_markdown_blocks` (texte → blocs typés titre1/titre2/paragraphe/puce, chacun porté par des segments `(texte, italique)`), `append_markdown_to_document` (rejoue les blocs sur un `Document` déjà chargé), `assemble_final_document` (charge le `.docx` existant, y ajoute le Markdown, écrit vers `<docx>_final.docx` par défaut — **n'écrase jamais le fichier d'entrée**, cohérent avec la consigne déjà actée de ne jamais écraser silencieusement un fichier dans `output/`).
  - **Points d'entrée**, même schéma que les autres opérations (jalon 28) : `scripts/assemble_document.py <docx> <markdown> [-o sortie]` (chemins explicites, pas de fixture par nom : les entrées sont deux fichiers déjà produits, pas des données de naissance) et un 4e outil MCP, `assemble_final_document`, dans `mcp_server.py`.
  - Validé par `tests/test_assembly.py` (parsing sur extraits synthétiques, assemblage sur `.docx`/Markdown de test avec vérification explicite que le fichier d'entrée n'est jamais modifié, chemin de sortie personnalisé, erreurs sur fichiers manquants) et `tests/test_mcp_server.py` (le nouvel outil nu). Vérifié aussi en conditions réelles : assemblage de `output/anthony.docx` + `output/anthony_phase3_draft.md` en `output/anthony_final.docx`, relu et confirmé — les 3 phases apparaissent dans l'ordre avec les bons niveaux de titre, l'emphase de la bibliographie est préservée.
  - Ceci clôt l'item 4 de Reste à faire.
- **Jalon 31 (terminé)** : ajout du fichier `LICENSE` (AGPL-3.0-or-later) — trouvé en discutant des implications de licence d'un Skill/Plugin/MCP pour « générer le document final ».
  - **Constat** : aucun des trois mécanismes (Skill, MCP local, Plugin) n'ajoute d'exposition réseau nouvelle (voir jalons 27-28 pour l'analyse déjà faite sur la clause AGPL §13, « Remote Network Interaction », qui ne se déclenche que pour un service hébergé publiquement — aucun des trois n'en est un). Mais discuter du Plugin (distribution/marketplace) a fait remonter une question différente et plus fondamentale, jamais vérifiée jusqu'ici : ce dépôt est **public sur GitHub** (`gh repo view` : `visibility: PUBLIC`) et **importe directement `pyswisseph` (AGPL-3.0)**, sans qu'aucune licence ne soit déclarée pour le code de ce dépôt lui-même (aucun fichier `LICENSE`, rien dans `pyproject.toml`). Recherche faite (pas une supposition) : selon la doctrine GPL/FSF généralement admise pour les dépendances Python, importer une bibliothèque GPL/AGPL et distribuer son propre code (un dépôt public cloneable en est une forme, indépendamment de tout service réseau) engage à distribuer ce code sous une licence compatible — généralement la même. C'était donc déjà une lacune avant ce jalon, indépendante de la question Skill/Plugin/MCP qui l'a fait remonter.
  - **`LICENSE`** : texte intégral et non modifié de la GNU AGPL-3.0 (récupéré directement depuis `gnu.org/licenses/agpl-3.0.txt`, jamais reformulé ni résumé — un texte de licence doit être reproduit tel quel), avec une seule ligne de copyright ajoutée en tête (« Copyright (C) 2026 AnGrau »), pratique standard qui ne modifie pas les termes eux-mêmes (vérifié par diff : le corps du texte est identique à l'original, seule la ligne de copyright est ajoutée avant).
  - **`pyproject.toml`** : `license = "AGPL-3.0-or-later"` (identifiant SPDX, PEP 639) — "or-later" plutôt que "only" pour rester cohérent avec la notice suggérée par la FSF elle-même en fin de texte de licence ("version 3 ... or (at your option) any later version"), sans raison particulière de se restreindre à la version 3 exactement.
  - **Ce que ça implique concrètement pour la suite** : toute distribution de ce code (dépôt public, Plugin en marketplace, etc.) doit rester sous AGPL-3.0-or-later ou compatible ; un service web/API public réel reste bloqué sur la question déjà actée (budgeter la licence Swiss Ephemeris Professional) — ceci ne change pas avec l'ajout du fichier `LICENSE`, qui ne fait que rendre explicite une situation déjà de fait.
  - Aucun test à écrire (fichier de licence, pas de logique) ; `uv sync` revérifié pour confirmer que la métadonnée `license` ne casse pas la résolution de dépendances.
- **Jalon 32 (terminé)** : skill d'orchestration de bout en bout, `.claude/skills/hellenistic-astrology/SKILL.md` — décidé après avoir mis de côté l'option Plugin (pas de besoin concret identifié : ce projet est lié à un seul dépôt, pas un outil générique réutilisable ailleurs, et un Plugin n'aiderait ni Mistral Vibe/Le Chat — qui ne consomment pas le format de bundle Claude-Code-spécifique — ni Claude Chat/Le Chat en tant que tels).
  - **Recherche complémentaire faite avant de trancher** : les « plugins » de Mistral (`docs.mistral.ai/studio-api/workflows/building-workflows/plugins`) sont un concept **sans rapport** avec les plugins Claude Code — des paquets Python pip-installables pour leur SDK d'orchestration Workflows/Studio (backend, développeurs), sans lien avec Vibe ni Le Chat. Ne change donc rien à l'analyse de portabilité déjà faite.
  - **Objectif retenu** : « large compatibilité » plutôt qu'un Plugin — un skill unique qui s'adapte à ce que l'environnement courant peut exécuter, plutôt que de dépendre d'un mécanisme d'installation supplémentaire.
  - **Conception** : 4 étapes (calcul + `.docx` Phase 1/2, brief de Phase 3, rédaction avec **arrêt obligatoire pour relecture humaine**, assemblage du document final), chacune avec une branche « outils MCP disponibles » (appel direct des 4 outils du jalon 28/30) et une branche « indisponibles » (Claude Chat, Mistral Le Chat sans logiciel local : la commande CLI/script exacte à lancer soi-même, résultat à coller dans la conversation). La relecture humaine de la Phase 3 (étape 3) est explicitement la seule étape que le skill ne doit **jamais** automatiser ni sauter, cohérent avec la décision du jalon 30.
  - Réutilise (par référence, pas duplication) les règles de rédaction déjà écrites dans le skill `hellenistic-astrology-phase3` (jalon 27) pour l'étape 3, plutôt que de les recopier — les deux skills vivent dans le même `.claude/skills/`.
  - Pas de nouveau code ni de nouveau test (fichier `SKILL.md`, pas de logique) ; `uv run pytest` reste vert par construction. Vérification prévue par usage réel (invoquer ce skill pour une demande de génération complète, avec et sans accès aux outils MCP).
- Voir la section **Reste à faire** ci-dessous pour la suite.

## Reste à faire

Roadmap explicite, dans l'ordre de priorité recommandé. Chaque jalon reste à valider avec l'utilisateur avant d'être attaqué (voir garde-fous ci-dessous) ; l'ordre ci-dessous est une recommandation, pas un engagement figé.

1. **Plugin Claude Code** — mis de côté explicitement (jalon 32) : ce projet est lié à un seul dépôt (le serveur MCP n'a de sens que contre ce code précis), donc « réutilisable dans d'autres projets » n'est pas un vrai bénéfice ici, et un Plugin n'aiderait ni Mistral Vibe/Le Chat (format de bundle spécifique à Claude Code) ni Claude Chat/Le Chat en tant que tels. À reconsidérer seulement si un besoin concret émerge (slash command, hooks Claude-Code-spécifiques).
2. **Serveur MCP distant/hébergé publiquement** (pour Claude Chat ou Mistral Le Chat directement, sans logiciel local) — explicitement **non engagé**, bloqué sur la décision de budgeter la licence Swiss Ephemeris Professional (voir Environnement de travail) avant toute mise en ligne, plus les questions d'hébergement (qui paie/maintient le serveur, sécurité des données de naissance transmises sur le réseau, disponibilité). Le serveur MCP local (jalon 28) et le skill d'orchestration (jalon 32) couvrent déjà Claude Code, Claude Desktop et Mistral Vibe sans ce risque.
3. **Docker** — **laissé de côté explicitement pour l'instant** (bénéfices discutés avec l'utilisateur) : aucun blocage technique (le CLI du jalon 5 remplit la condition initiale), mais aucun besoin actif non plus. Le seul bénéfice concret identifié (faciliter l'usage par quelqu'un d'autre sans installer Python/`uv`) ne correspond à aucun besoin présent ; la raison d'origine (« en vue d'une exposition future en service ») reste bloquée sur la question de licence Swiss Ephemeris Professional (item 2). Point notable, à garder en tête si reconsidéré un jour : dockeriser dégraderait l'intégration MCP actuelle (jalons 28/30/32), qui repose sur un sous-processus local léger (`uv run python -m hellenistic_astrology.mcp_server`) — un conteneur ajouterait de la latence de démarrage et exigerait des volumes montés (`output/`, `data/ephe/`) pour ne pas perdre l'état entre deux invocations.

## Cas de test de référence (vérité terrain)

Deux thèmes ont déjà été analysés manuellement et validés : ils servent de fixtures de non-régression pour tout calcul automatisé.

- **Anthony** — 20 novembre 1970, 23h10 (CET), Paris 14e, France (48°49'59" N, 2°19'36" E). Thème nocturne. Part de Fortune attendue : Scorpion 20°08'. Ascendant Lion, MC Taureau.
- **Liam** — 19 octobre 2005, 15h50 (CEST), Vitry-sur-Seine, France (48°47' N, 2°24' E). Thème diurne. Part de Fortune attendue : Lion 27°60' (= 28°00', notation normalisée puisque 60' = 1°, confirmé par l'utilisateur). Ascendant Verseau, MC Sagittaire.

Tout module de calcul (positions, maisons, secte, dignités, Lots, aspects) doit être testé contre ces deux thèmes avant d'être considéré fiable.

**Ces données de naissance ne sont jamais poussées sur GitHub** : `/References` (les `.docx` sources), `/tests/fixtures` (les valeurs transcrites en JSON) et `/output` (les `.docx` générés par `scripts/generate_docx.py` ou le CLI) sont dans `.gitignore`. Le code des tests (`tests/test_regression_*.py`, `tests/test_timezone.py`, `tests/test_dignities.py`, `tests/test_sect.py`, `tests/test_docgen.py`, `tests/test_aspects.py`, `tests/test_cli.py`, `tests/test_geocoding.py`, `tests/test_bibliography.py`) reste versionné ; seules les données personnelles qu'il consomme ou produit restent locales. Les tests de géocodage mockent l'appel réseau (`unittest.mock`) : aucun lieu réel n'est envoyé à Nominatim pendant `pytest`.

## Ce que Claude Code ne doit pas faire sans validation explicite

- Ne pas modifier les réglages astrologiques listés ci-dessus (ce sont des choix méthodologiques assumés par l'utilisateur, pas des paramètres à optimiser).
- Ne pas remplacer les livres de référence ni en ajouter d'autres sans demande explicite.
- Ne pas publier ou committer de données personnelles de tiers (dates de naissance, lieux) au-delà des deux cas de test ci-dessus, qui ont déjà été partagés volontairement par l'utilisateur — et même pour ces deux cas, ne jamais retirer `/References`, `/tests/fixtures` ou `/output` du `.gitignore` sans demande explicite.
- Ne jamais supprimer de fichiers dans `/output` : l'utilisateur les réutilise manuellement dans des sessions Claude Chat et Mistral Le Chat (Vibe) — ce ne sont pas de simples artefacts jetables. Les régénérer (écraser via `scripts/generate_docx.py`/`generate_brief.py` ou une nouvelle rédaction) est normal et attendu ; les effacer ou les remplacer par un contenu différent sans demande explicite ne l'est pas.
- Ne pas choisir seul l'ordre ou le périmètre des items listés dans "Reste à faire" sans validation explicite — la priorisation proposée est une recommandation, pas un mandat. En particulier, ne pas décider seul de l'approche pour un item structurant à venir : ce sont des choix qui méritent leur propre discussion de planification, comme cela a déjà été fait pour la Phase 3 (jalon 26) et pour Skill vs plugin (jalon 27).
- Pour tout calcul sans fixture de vérité terrain (Anthony/Liam ne couvrent pas tout — ex. triplicité, bornes, décans) : vérifier contre au moins deux sources indépendantes et cohérentes entre elles avant d'écrire la table, plutôt que de coder une valeur incertaine de mémoire. Le signaler explicitement dans le résumé et dans CLAUDE.md (voir jalon 11).
