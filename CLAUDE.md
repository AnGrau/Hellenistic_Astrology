# CLAUDE.md

Ce fichier donne à Claude Code le contexte du projet. Le lire entièrement avant toute action.

## Objectif du projet

Outiller la production d'analyses de thèmes natals en astrologie hellénistique (signes entiers), pour remplacer progressivement la saisie manuelle des données Astro-Seek/Astro.com et la génération manuelle du document Word par un pipeline reproductible :

1. calcul des positions et facteurs du thème (idéalement via une éphéméride locale, sans dépendre d'une capture d'écran),
2. dérivation automatique des données de la phase d'observation (maisons en signes entiers, secte, dignités essentielles, aspects ptoléméens par signe, Lots grecs, libération zodiacale),
3. génération du document `.docx` final selon la structure déjà validée sur les thèmes d'Anthony et de Liam.

Le dépôt sera versionné sur GitHub.

## Réglages astrologiques (non négociables, à appliquer systématiquement)

- Système de maisons : signes entiers (Whole Sign).
- Zodiaque tropical.
- Aspects ptoléméens uniquement (conjonction, sextile, carré, trigone, opposition), considérés par signe plutôt que par degré d'orbe — plus l'écart en degré est faible, plus l'aspect est actif.
- Aspects hors-signe ignorés, sauf si la planète la plus rapide applique à moins de 3° à la planète la plus lente. Cette proximité se mesure sur l'écart réel en longitude écliptique (les deux planètes doivent être proches de la frontière commune entre leurs signes adjacents), pas sur une coïncidence de degré-dans-signe : Mercure à 11°57' Sagittaire et Vénus à 11°51' Scorpion ont des degrés-dans-signe presque identiques mais restent à ~30° d'écart réel, car aucun des deux n'est proche de la frontière Scorpion/Sagittaire (cas vérifié sur le thème d'Anthony — la règle ne s'active pas).
- Solaire (retour solaire) : lieu de naissance, Soleil non précessé.
- Secte de la carte déterminée par la position du Soleil au-dessus ou au-dessous de l'horizon ; la Part de Fortune et la Part de l'Esprit changent de formule selon secte diurne/nocturne (piège fréquent à tester explicitement).
- Rôle de secte par planète : règle symétrique retenue pour le luminaire « en désaccord » avec la secte de la carte — toujours « Hors secte (jour) » ou « Hors secte (nuit) », jamais « Neutre » (un des deux thèmes de référence contenait une asymétrie sur ce point, corrigée après validation explicite).

## Méthode de rédaction en trois phases (à respecter dans tout générateur de texte ou de document)

1. **Observation** : relevé factuel des positions, secte, dignités essentielles, rétrogradations, planètes sous les rayons, aspects par signe — sans interprétation.
2. **Rédaction** : fiche technique descriptive (répartition élémentaire et modale, angularité, Ascendant et son maître, luminaires, dignités et réceptions, nœuds et Parts, structure du thème).
3. **Interprétation** : synthèse destinée au client (orientation générale, Ascendant et son maître, luminaires, structure du thème, nuances de secte/dignité, synthèse finale), avec section optionnelle sur les repères temporels actuels (libération zodiacale) si les dates le permettent.

## Style des textes générés

- Ton bienveillant mais factuel, jamais péremptoire sur la vie de la personne (formulations du type « invite à », « suggère », « mérite d'être vérifié avec la personne concernée »).
- Aucune émoticône.
- Toute affirmation technique (dignité, aspect, secte) doit être vérifiable depuis les données d'observation de la phase 1 — pas d'interprétation qui devance les faits.
- Bibliographie systématique en fin de document, avec URLs vérifiées à la date de génération (voir ci-dessous).

## Références bibliographiques de travail

- Chris Brennan, *Hellenistic Astrology: The Study of Fate and Fortune* — https://theastrologypodcast.com/books/
- Demetra George, *Ancient Astrology in Theory and Practice: A Manual of Traditional Techniques, Volume I: Assessing Planetary Condition* (Rubedo Press, 2019) — https://rubedo.press/ancient-astrology
- Demetra George, *Ancient Astrology in Theory and Practice: A Manual of Traditional Techniques, Volume II: Delineating Planetary Meaning* (Rubedo Press, 2022) — https://rubedo.press/ancient-astrology-volume-two
- Demetra George, *Astrology for Yourself* et *Astrology and the Authentic Self* — https://demetra-george.com/books/

Ces URLs pointent vers les pages officielles (éditeur ou auteur/autrice). Si le projet les republie (README, docs), revérifier leur validité avant chaque publication plutôt que de les recopier sans contrôle.

## Environnement de travail

- Développement exclusivement sur PC (pas de mobile, pas de dépendance à un environnement cloud propriétaire).
- Langage principal : Python (`uv` pour l'environnement/dépendances), layout `src/hellenistic_astrology/`.
- Éphéméride : **pyswisseph** (Swiss Ephemeris). Licence AGPL-3.0 acceptée pour l'instant ; si un service web/API public est un jour lancé, budgéter la licence commerciale Swiss Ephemeris Professional avant la mise en ligne. Fichiers de données `.se1` téléchargés via `scripts/fetch_ephemeris.py` vers `data/ephe/` (gitignored, jamais committés) ; en leur absence, le calcul bascule automatiquement sur la théorie Moshier/Moseph (précision ~1 arcseconde, suffisante pour les tests).
- Fuseau horaire : résolution automatique lieu + heure locale via `zoneinfo`, fiable à partir de 1916 (introduction de l'heure d'été en France) ; avant cette date, un datetime UTC explicite est obligatoire (voir `core/timezone.py`).
- Génération du `.docx` : décision actée d'utiliser **python-docx** (pas de sous-processus Node) — le script Node/docx-js évoqué initialement n'a jamais existé dans ce dépôt.
- CLI : `hellenistic-astrology <fichier-birth-data.json>` (entry point `src/hellenistic_astrology/cli.py`, déclaré dans `pyproject.toml`), voir `README.md`.
- Géocodage (`core/geocoding.py`) : opt-in uniquement — `latitude`/`longitude` explicites sont toujours prioritaires et n'entraînent aucun appel réseau ; le champ `place` (texte libre) n'est résolu via Nominatim/OpenStreetMap que s'ils sont absents. Invariant à préserver : ne jamais géocoder silencieusement quand des coordonnées directes existent déjà.
- Docker : prévu (en vue d'une exposition future en service) ; le CLI ci-dessus lève le seul blocage technique, mais l'ajout reste une décision à prendre explicitement (voir Reste à faire).

## Avant tout commit et push sur GitHub

Avant de committer et pousser, relire et mettre à jour si le jalon en cours l'exige :

- **CLAUDE.md** — au minimum la section État d'avancement (nouveau jalon) et, si le périmètre a changé, Reste à faire.
- **README.md** — si l'usage du CLI ou une fonctionnalité visible par l'utilisateur a changé.
- **pyproject.toml** — si des dépendances ou la version ont changé.
- **Docs référencées depuis CLAUDE.md** (ex. `docs/table_termes_egyptiens.md`) — si leur contenu est concerné par le jalon.
- **Système de mémoire persistante** (`~/.claude/projects/.../memory/`, hors dépôt Git, jamais poussé sur GitHub) — mémoires feedback/project/user pertinentes au jalon.

C'est une consigne à appliquer soi-même, pas un garde-fou mécanique (pas de hook technique bloquant `git commit`/`git push` — décision explicite de l'utilisateur).

## État d'avancement

- **Jalon 1 (terminé)** : cœur de calcul (`src/hellenistic_astrology/core/`) — positions planétaires, maisons en signes entiers, secte, Part de Fortune/Esprit, résolution de fuseau horaire. Validé par `tests/test_regression_anthony.py` et `tests/test_regression_liam.py` (écarts sous 0,5' d'arc par rapport aux thèmes de référence).
- **Jalon 2 (terminé)** :
  - 2a — `core/dignities.py` (dignités essentielles domicile/exaltation/exil/chute/pérégrin, au niveau du signe ; table des maîtrises traditionnelles) et `core/sect.py` étendu avec le rôle de secte par planète.
  - 2b — `docgen/builder.py` génère la Phase 1 (Observation) en `.docx` via python-docx (tableaux positions + maîtrises), à partir de l'objet `Observation` uniquement. `scripts/generate_docx.py` permet une génération manuelle vers `output/` (gitignored). Phases 2 et 3 (rédaction, interprétation) restent hors périmètre de docgen : ce sont des tâches de génération de texte, pas de mise en forme de données.
  - Validé par `tests/test_dignities.py`, `tests/test_sect.py`, `tests/test_docgen.py` en plus des tests de régression.
- **Jalon 3 (terminé)** : `core/aspects.py` — regroupement des points en amas par signe, aspect ptoléméen par signe entre chaque paire d'amas, règle des 3° hors-signe basée sur la proximité écliptique réelle. Les 31 relations d'amas des deux thèmes (10 pour Anthony, 21 pour Liam) ont été vérifiées à la main contre le texte "Aspects par signe relevés" des documents de référence. Validé par `tests/test_aspects.py` et les tests de régression étendus.
  - **Limite connue** : aucun des deux thèmes de référence ne déclenche la règle des 3° (les deux documents confirment explicitement qu'elle ne s'active pas) — le cas positif n'est couvert que par un test synthétique, sans validation sur données réelles. À surveiller si un futur thème la déclenche.
- **Jalon 4 (terminé)** : `docgen/builder.py` génère la section "Aspects par signe relevés" en puces factuelles — une puce de conjonction par amas (accord grammatical masculin/féminin selon les membres), puis une puce par paire d'amas pour chaque relation d'aspect, aversions incluses. Format volontairement plus simple et déterministe que la prose des deux documents de référence, qui divergeaient stylistiquement entre eux sur ce point. Commentaire interprétatif (significations de maîtrise, etc.) toujours hors périmètre de docgen.
- **Jalon 5 (terminé)** : CLI réel (`src/hellenistic_astrology/cli.py`, entry point `hellenistic-astrology` déclaré dans `pyproject.toml`) — prend en entrée un fichier JSON de données de naissance (schéma libre, name/latitude/longitude + local_date/local_time/tz_name ou utc_datetime) et génère le `.docx` vers `output/<nom-slugifié>.docx` par défaut. `main.py` (scaffold `uv` mort, jamais branché) a été supprimé. Testé de bout en bout avec un client synthétique en plus des deux fixtures. Usage documenté dans `README.md`.
- **Jalon 6 (terminé)** : géocodage opt-in (`core/geocoding.py`) — un champ `place` (texte libre) en alternative à `latitude`/`longitude` dans le JSON du CLI, résolu via l'API Nominatim/OpenStreetMap (dépendance réseau, envoie le lieu à ce service tiers). `latitude`/`longitude` restent toujours prioritaires si fournis : aucun appel réseau dans ce cas, comportement par défaut inchangé. Testé avec des appels réseau mockés (`tests/test_geocoding.py`) et vérifié une fois en conditions réelles.
- **Jalon 7 (terminé)** : désambiguïsation du géocodage — `geocode()` demande 5 résultats à Nominatim et refuse (avec la liste des candidats) si plusieurs **lieux réellement distincts** sont trouvés, plutôt que de deviner en prenant le premier. "Distincts" est déterminé par une distance approximative (< 5 km = même lieu) plutôt qu'un simple comptage brut : test manuel en conditions réelles, "Paris, France" et "Paris 14e, France" renvoient chacun plusieurs entrées OSM quasi-identiques (ville + département, ou plusieurs commerces du même quartier) qui auraient déclenché une fausse ambiguïté sans ce filtre. "Paris" seul (sans pays) reste correctement détecté comme ambigu (France vs Texas). Nouveau champ optionnel `country_code` (ISO 3166-1 alpha-2) transmis à Nominatim pour lever l'ambiguïté en amont. Validé par `tests/test_geocoding.py` (dont un test dédié à la non-régression sur les faux positifs de proximité) et un test manuel en conditions réelles.
- **Jalon 8 (terminé)** : Nœud Nord/Sud (`ephemeris.north_node`, nœud **vrai** TRUE_NODE — le nœud moyen ne stationne jamais, incompatible avec les directions "Direct (rare)"/"Rétrograde (usuel)" observées dans les deux thèmes de référence) et Part d'Éros (`lots.part_of_eros`, formule Ascendant+Vénus−Esprit/Ascendant+Esprit−Vénus selon la secte, vérifiée exactement contre les deux thèmes). `Observation` gagne `north_node`/`south_node`/`part_of_eros` et un champ `all_points` (ordre canonique d'affichage, source unique pour docgen et pour le calcul des amas/aspects — refactor qui a remplacé la reconstruction manuelle de liste dans `docgen/builder.py`). Nœud Sud ajouté au cluster Lion d'Anthony bien que le document source ne le mentionne pas explicitement dans le texte d'aspects (incohérence avec Liam, qui mentionne "Nœud Sud" — traité comme une omission du document original plutôt que reproduit). Les 15 (Anthony) et 28 (Liam) relations d'amas ont été revérifiées à la main contre le texte des deux documents avant d'écrire les fixtures. Validé par les tests de régression étendus et `tests/test_docgen.py`.
- **Jalon 9 (terminé)** : réceptions mutuelles par domicile (`dignities.mutual_receptions_by_domicile`) — détecte les paires de planètes classiques chacune dans le domicile de l'autre. Rendu en `docgen` dans la section "Aspects par signe relevés" (même emplacement que le document source), phrasé simplement ("Réception mutuelle par domicile entre X et Y."). **Découverte en vérifiant contre les deux thèmes** : le calcul reproduit exactement l'unique réception confirmée d'Anthony (Mars/Vénus, aussi listée dans sa section Phase 2 "Dignités et réceptions"), mais détecte en plus une réception Vénus/Jupiter chez Liam (Vénus en Sagittaire, domicile de Jupiter ; Jupiter en Balance, domicile de Vénus) que la section "Dignités et réceptions" du document source de Liam ne mentionne pas du tout. Vérifiée indépendamment par calcul et retenue dans la fixture comme une omission probable du document original plutôt que reproduite à l'identique — cohérent avec la méthode déjà appliquée aux autres incohérences trouvées dans ces documents (secte Soleil/Lune, Nœud Sud d'Anthony). Validé par `tests/test_dignities.py` et les tests de régression étendus.
- **Jalon 10 (terminé)** : vérification bibliographique (`hellenistic_astrology/bibliography.py` + `scripts/check_bibliography.py`) — HEAD sur les 4 URLs de la section "Références bibliographiques de travail" de CLAUDE.md, avec repli en GET si le serveur ne supporte pas HEAD (405/501). Les 4 URLs sont codées en dur dans `bibliography.py` (pas de parsing de CLAUDE.md à l'exécution) mais `tests/test_bibliography.py::test_references_match_claude_md` lit CLAUDE.md à chaque run de test pour empêcher toute dérive entre les deux listes. Testé avec des appels réseau mockés et vérifié une fois en conditions réelles : les 4 URLs répondent, HEAD fonctionne directement (repli GET jamais déclenché). Script autonome, pas encore branché sur le CLI/docgen — la génération d'une vraie section "Bibliographie" dans le `.docx` n'existe pas encore (pas de fin de document tant que les Phases 2/3 ne sont pas traitées).
- **Jalon 11 (terminé)** : maîtres de triplicité (`dignities.triplicity_dignity`, `dignities.TRIPLICITY_RULERS`) — système **dorothéen** (Dorothée de Sidon, tel que documenté par Chris Brennan), distinct du système ptoléméen simplifié qui unifie le maître Eau jour/nuit sur Mars sans maître participant. **Aucune fixture de vérité terrain pour ce calcul** : ni le thème d'Anthony ni celui de Liam ne documentent la triplicité dans leurs documents de référence — vérifié à la place contre deux sources indépendantes et cohérentes entre elles (Wikipedia "Triplicity", Augurine) avant d'écrire la table, en particulier pour lever une incertitude réelle sur le maître participant de la triplicité d'Eau (Lune). Nouveau champ `PointPosition.triplicity_dignity`, calculé pour les 7 planètes classiques uniquement (comme `essential_dignity`), sans dépendance à la secte de la carte (les trois maîtres jour/nuit/participant sont tous considérés dignifiés ; la secte ne fait que hiérarchiser leur poids interprétatif, hors périmètre ici). **Pas encore rendu dans docgen** : la table des positions existante correspond exactement au tableau des documents de référence (qui n'inclut pas la triplicité), donc y ajouter une colonne est un choix de présentation à trancher séparément (voir Reste à faire). Validé par `tests/test_dignities.py` (5 tests, un par élément + un cas négatif) et une inspection manuelle informative des deux thèmes (non vérifiable, juste cohérente).
- **Jalon 12 (terminé)** : bornes égyptiennes (`dignities.EGYPTIAN_BOUNDS`, `dignities.egyptian_bound_ruler`, `dignities.bound_dignity`) — division de chaque signe en 5 segments inégaux (Vettius Valens, *Anthologiae* Livre I). **Recherche web infructueuse** pour obtenir une table complète et recoupable (blocage documenté avant ce jalon) ; **table finalement fournie et vérifiée par l'utilisateur** dans `docs/table_termes_egyptiens.md` (déplacé depuis `data/`, qui est réservé au cache d'éphéméride gitignored), avec ses propres sources et recoupements. Les 12 lignes totalisent chacune 30° (vérifié automatiquement par `tests/test_dignities.py::test_egyptian_bounds_each_sign_totals_30_degrees`). Même limite que la triplicité : aucune fixture Anthony/Liam ne documente les bornes, inspection manuelle des deux thèmes informative seulement. Nouveau champ `PointPosition.bound_dignity`, calculé pour les 7 planètes classiques.
- **Jalon 13 (terminé)** : décans/faces (`dignities.CHALDEAN_ORDER`, `dignities.decan_ruler`, `dignities.decan_dignity`) — division de chaque signe en 3 segments de 10°, maîtres selon l'ordre chaldéen (Saturne, Jupiter, Mars, Soleil, Vénus, Mercure, Lune), qui cycle en continu à travers les 36 décans du zodiaque sans se réinitialiser à chaque signe (Bélier 1er décan = Mars, tel qu'utilisé par Vettius Valens et Hephaistion). Contrairement aux bornes égyptiennes, la recherche web a abouti cette fois : vérifié contre deux sources indépendantes et cohérentes (une synthèse citant Valens/Hephaistion, une table complète des 36 décans d'Augurine), les 36 décans recoupés avant écriture du code. Même limite que triplicité/bornes : aucune fixture Anthony/Liam ne documente les décans. Nouveau champ `PointPosition.decan_dignity`, calculé pour les 7 planètes classiques. Validé par `tests/test_dignities.py` (36 décans paramétrés + limites + cyclage inter-signes) et une inspection manuelle informative des deux thèmes.
- **Jalon 14 (terminé)** : rendu docgen des dignités mineures — nouvelle table "Dignités mineures (triplicité, bornes, décans)" (`docgen.builder.add_minor_dignities_table`), insérée entre "Maîtrises traditionnelles" et "Aspects par signe relevés". Une ligne par planète classique, colonnes Triplicité/Terme/Décan, "—" pour les cellules sans dignité mineure (cohérent avec la convention déjà utilisée dans la table des positions). Table séparée plutôt qu'une extension de la table des positions existante, qui reste identique au tableau des documents de référence. Validé par un test unitaire dédié (valeurs synthétiques) et par le test de structure du document, qui recalcule les valeurs attendues via les mêmes fonctions `core.dignities` appliquées à la position réellement calculée (teste le branchement docgen, pas l'exactitude astrologique déjà couverte par `tests/test_dignities.py`).
- **Jalon 15 (terminé)** : libération zodiacale, calcul seul (`core/zodiacal_releasing.py`) — Part de Fortune uniquement, niveaux L1 à L4 complets, sans les raffinements de Valens (relâchement du lien, périodes culminantes) et sans rendu docgen (voir Reste à faire pour ces trois points, différés à la demande explicite de l'utilisateur). Table `ZODIACAL_RELEASING_YEARS` (années fixes par signe) et convention d'année « égyptienne » de 360 jours (12 mois de 30 jours, chaque niveau divisant l'unité du niveau parent par 12) vérifiées par recoupement de 3 sources indépendantes et cohérentes entre elles : transcription du podcast de Chris Brennan (épisode 192, citant directement Vettius Valens), calculateur Augurine, synthèse kerykeion — dont un exemple chiffré du podcast (période L1 Cancer de 25 ans débutant le 1/1/2012 se terminant le 22/8/2036, à cause des années de 360 jours) repris tel quel en test de non-régression. Point notable : Capricorne (27 ans) et Verseau (30 ans) ont des durées différentes en libération zodiacale bien que tous deux domicile de Saturne — particularité documentée de la technique, pas une erreur de recopie. Structure auto-similaire : `level_periods()` découpe un intervalle en périodes de signes consécutifs (cyclant plusieurs fois autour du zodiaque si nécessaire, dernière période tronquée pour boucler exactement) ; `sub_periods()` applique la même logique au niveau inférieur en partant toujours du signe du parent ; `releasing_tree()` calcule récursivement les niveaux 1 à `max_level`. Aucune fixture Anthony/Liam ne documente cette technique (comme pour triplicité/bornes/décans) ; validé par `tests/test_zodiacal_releasing.py` (table des années, exemple chiffré du podcast, invariants de partition exacte à tous les niveaux, cyclage multi-tour, recherche de période active).
- **Jalon 16 (terminé)** : libération zodiacale depuis la Part de l'Esprit — extension directe du jalon 15 : `core/zodiacal_releasing.py` était déjà générique sur le signe de départ (`releasing_tree(lot_sign, ...)` ne fait aucune hypothèse sur Fortune spécifiquement), donc aucun changement de code n'était nécessaire pour l'algorithme lui-même. Le travail de ce jalon est la couverture de test explicite de cet usage : `tests/test_zodiacal_releasing.py::test_l1_from_part_of_spirit_reference_charts`, paramétré sur les deux thèmes de référence (Esprit d'Anthony en Taureau, Esprit de Liam en Cancer), utilisant leur date de naissance réelle comme point de départ. Même limite que le jalon 15 : aucune fixture ne documente de périodes de libération zodiacale attendues, ces tests valident le branchement (signe de départ, date de naissance), pas des dates de résultat vérifiées indépendamment. Raffinements de Valens et rendu docgen restent différés (voir Reste à faire).
- Voir la section **Reste à faire** ci-dessous pour la suite.

## Reste à faire

Roadmap explicite, dans l'ordre de priorité recommandé. Chaque jalon reste à valider avec l'utilisateur avant d'être attaqué (voir garde-fous ci-dessous) ; l'ordre ci-dessous est une recommandation, pas un engagement figé.

1. **Libération zodiacale — raffinements de Valens** : relâchement du lien (« loosing of the bond ») et périodes culminantes (angularité du sous-maître) — nouvelle logique, aucune source encore recoupée (voir garde-fous : vérifier contre au moins deux sources indépendantes avant d'écrire la table/l'algorithme, comme pour le jalon 15).
2. **Libération zodiacale — rendu docgen** : intégrer les périodes déjà calculées (Fortune, jalon 15 ; Esprit, jalon 16) dans le document généré — suppose de décider d'abord une date de référence (« aujourd'hui ») et le niveau de détail à afficher (L1 seul ? L1+L2 ?).
3. **Phase 2 — Fiche technique** (répartition élémentaire/modale, angularité, Ascendant et son maître, luminaires, dignités et réceptions, nœuds et Parts, structure du thème) — mélange tableau (élémentaire/modale, déjà vu dans les références) et texte descriptif. Premier vrai changement de nature par rapport aux jalons précédents : ce n'est plus seulement de la mise en forme mécanique de données calculées.
4. **Phase 3 — Interprétation** (synthèse, nuances de secte/dignité, section optionnelle libération zodiacale) — rédaction pure destinée au client. À ce stade, `docgen` au sens actuel (mise en forme déterministe d'un objet `Observation`) atteint sa limite : c'est une tâche de génération de texte assistée, probablement un prompt structuré plutôt que du code Python déterministe — à repenser en phase de planification dédiée plutôt que de trancher seul.
5. **`docgen/CLAUDE.md` scopé** — évoqué à deux reprises sans être tranché ; le bon moment est l'attaque de l'item 3 ou 4, puisque c'est le changement de nature de tâche (texte vs données) qui justifierait un fichier séparé.
6. **Skill Claude** (`skills/hellenistic-astrology/SKILL.md`) — wrapper fin autour du CLI (jalon 5, terminé), idéalement après que les Phases 2/3 existent au moins partiellement.
7. **Docker** — prévu ; sa condition initiale (un point d'entrée réel) est désormais remplie par le CLI du jalon 5, donc plus aucun blocage technique, mais toujours pas engagé sans décision explicite.
8. **MCP** — uniquement si un besoin réel émerge d'un usage via Claude Desktop ou similaire ; non engagé.

## Cas de test de référence (vérité terrain)

Deux thèmes ont déjà été analysés manuellement et validés : ils servent de fixtures de non-régression pour tout calcul automatisé.

- **Anthony** — 20 novembre 1970, 23h10 (CET), Paris 14e, France (48°49'59" N, 2°19'36" E). Thème nocturne. Part de Fortune attendue : Scorpion 20°08'. Ascendant Lion, MC Taureau.
- **Liam** — 19 octobre 2005, 15h50 (CEST), Vitry-sur-Seine, France (48°47' N, 2°24' E). Thème diurne. Part de Fortune attendue : Lion 27°60' (= 28°00', notation normalisée puisque 60' = 1°, confirmé par l'utilisateur). Ascendant Verseau, MC Sagittaire.

Tout module de calcul (positions, maisons, secte, dignités, Lots, aspects) doit être testé contre ces deux thèmes avant d'être considéré fiable.

**Ces données de naissance ne sont jamais poussées sur GitHub** : `/References` (les `.docx` sources), `/tests/fixtures` (les valeurs transcrites en JSON) et `/output` (les `.docx` générés par `scripts/generate_docx.py` ou le CLI) sont dans `.gitignore`. Le code des tests (`tests/test_regression_*.py`, `tests/test_timezone.py`, `tests/test_dignities.py`, `tests/test_sect.py`, `tests/test_docgen.py`, `tests/test_aspects.py`, `tests/test_cli.py`, `tests/test_geocoding.py`, `tests/test_bibliography.py`) reste versionné ; seules les données personnelles qu'il consomme ou produit restent locales. Les tests de géocodage mockent l'appel réseau (`unittest.mock`) : aucun lieu réel n'est envoyé à Nominatim pendant `pytest`.

## Ce que Claude Code ne doit pas faire sans validation explicite

- Ne pas modifier les réglages astrologiques listés ci-dessus (ce sont des choix méthodologiques assumés par l'utilisateur, pas des paramètres à optimiser).
- Ne pas remplacer les livres de référence ni en ajouter d'autres sans demande explicite.
- Ne pas publier ou committer de données personnelles de tiers (dates de naissance, lieux) au-delà des deux cas de test ci-dessus, qui ont déjà été partagés volontairement par l'utilisateur — et même pour ces deux cas, ne jamais retirer `/References`, `/tests/fixtures` ou `/output` du `.gitignore` sans demande explicite.
- Ne pas choisir seul l'ordre ou le périmètre des items listés dans "Reste à faire" sans validation explicite — la priorisation proposée est une recommandation, pas un mandat. En particulier, ne pas décider seul de l'approche pour les Phases 2/3 (items 2/3) : ce sont des choix qui méritent leur propre discussion de planification.
- Pour tout calcul sans fixture de vérité terrain (Anthony/Liam ne couvrent pas tout — ex. triplicité, bornes, décans) : vérifier contre au moins deux sources indépendantes et cohérentes entre elles avant d'écrire la table, plutôt que de coder une valeur incertaine de mémoire. Le signaler explicitement dans le résumé et dans CLAUDE.md (voir jalon 11).
